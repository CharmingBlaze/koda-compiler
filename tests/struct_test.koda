struct point { x, y }

let p = point { x: 3, y: 4 };

assert(p.x == 3, "struct field x");
assert(p.y == 4, "struct field y");
p.y = 10;
assert(p.y == 10, "struct field assign");
print("PASS: struct");
