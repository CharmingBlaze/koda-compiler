let obj = { a: 10, b: 20 };

print("Object for-of pairs:");

for (let k of ["a", "b"]) {
    print(k, ":", obj[k]);
}

let arr = ["X", "Y"];

print("Array indexed pairs:");

let i = 0;

while (i < len(arr)) {
    print(i, ":", arr[i]);
    i = i + 1;
}
