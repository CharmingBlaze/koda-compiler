#include "../stdlib/timer.koda"

let t = create(1.0);

let dt = 0.25;

while (!t.done) {
    t = update(t, dt);
}

assert(t.done, "timer finished");
assert(t.elapsed == 1.0, "elapsed at duration");
print("PASS: timer");
