func main() {
    let sink = [];
    let i = 0;
    while (i < 3000) {
        sink.push({ n: i, a: [i, i + 1] });
        if (len(sink) > 120) {
            sink = [];
        }
        gcframestep(0.1);
        i = i + 1;
    }
    print("PASS stress_mixed_alloc");
}
