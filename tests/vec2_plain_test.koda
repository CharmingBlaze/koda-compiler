#include "../stdlib/vec2.koda"

func main() {
    let a = create(3.0, 4.0);
    assert(a.x == 3.0, "x");
    assert(a.y == 4.0, "y");
    assert(length(a) == 5.0, "length");
    print("PASS vec2_plain");
}
