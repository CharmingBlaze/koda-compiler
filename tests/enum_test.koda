enum dir { up, down, left, right }

let u = dir.up;

let r = dir.right;

assert(u == 0, "enum Up");
assert(r == 3, "enum Right");

switch (u) {
    case dir.up:
        print("case up");
    default:
        print("default arm");
}

print("PASS: enum");
