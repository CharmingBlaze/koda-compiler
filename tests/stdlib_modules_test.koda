#include "@color"
#include "@easing"

func main() {
    let c = rgba(255, 0, 0, 255);
    assert(len(toHex(c)) > 0, "toHex");

    assert(easeIn(0.5) == 0.25, "easeIn");
    assert(easeOut(0) == 0, "easeOut edge");

    print("PASS stdlib_modules_test");
}
