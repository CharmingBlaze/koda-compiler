# {{name}}

A small **text-based game** built with Koda — no Raylib or native libraries required.

## Run

```bash
koda run
```

## Build

```bash
koda build -o {{name}}
./{{name}}
```

## Next steps

- Tweak physics in `src/main.koda` (fuel, thrust, landing speed).
- Try `koda new mygraphics --template graphics` for a windowed Raylib demo.
- Read `language.md` and `docs/guides/game-dev.md` in the Koda SDK.
