# Minimal Koda bindings

Auto-generated by **kodawrap** 0.2.0-dev from `minimal.h`. **2** functions wrapped.

## Quick start

```koda
#include "@minimal"

func main() {
    // Call library functions by name after linking wrapper.c
}
```

## Building

Set **`KODA_NATIVE_SOURCES`** to **`wrapper.c`** and **`KODA_LINKFLAGS`** so Clang finds the vendor headers and library, then:

```powershell
$env:KODA_NATIVE_SOURCES = ".\wrapper.c"
$env:KODA_LINKFLAGS = "-I<include> -L<lib> -lminimal"
koda build game.koda -o game.exe
```

```bash
export KODA_NATIVE_SOURCES="$PWD/wrapper.c"
export KODA_LINKFLAGS="-I<include> -L<lib> -lminimal"
koda build game.koda -o game
```

## Function categories

| Category | Count | Description |
|----------|-------|-------------|
| Miscellaneous | 2 | Auto-grouped from name prefixes. |

## Full API reference

See [api_reference.md](api_reference.md).

## License

These bindings are generated. The underlying **minimal** library retains its own license — see the upstream header.
