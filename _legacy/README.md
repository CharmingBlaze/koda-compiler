# Legacy reference artifacts

**Not part of the Koda build, CI, or release SDK.**

This directory holds old wrapper-generator output and one-off experiments kept for historical reference only. New contributors should ignore it — use `wrappers/` and `third_party/` for current Raylib integration.

These files may be removed in a future release; do not add new content here.
