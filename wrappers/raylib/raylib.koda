// ============================================================
//  raylib.koda
//  Auto-generated by kodawrap 0.4.0
//  Source:  raylib.h
//  Generated: 2026-06-15T08:55:05Z
// ============================================================
//
//  Usage:
//    #include "@raylib"
//
//  Compile:
//    set KODA_NATIVE_SOURCES=wrapper.c
//    set KODA_LINKFLAGS=-I<inc> -L<lib> -lraylib
//    koda build game.koda -o game.exe
// ============================================================

// ------------------------------------------------------------
//  Structs
// ------------------------------------------------------------

// Struct: Vector2
//   x: float
//   y: float

// Struct: Vector3
//   x: float
//   y: float
//   z: float

// Struct: Vector4
//   x: float
//   y: float
//   z: float
//   w: float

// Struct: Matrix
//   m12: float m0, m4, m8,
//   m13: float m1, m5, m9,
//   m14: float m2, m6, m10,
//   m15: float m3, m7, m11,

// Struct: Color
//   r: unsigned char
//   g: unsigned char
//   b: unsigned char
//   a: unsigned char

// Struct: Rectangle
//   x: float
//   y: float
//   width: float
//   height: float

// Struct: Image
//   data: void*
//   width: int
//   height: int
//   mipmaps: int
//   format: int

// Struct: Texture
//   id: unsigned int
//   width: int
//   height: int
//   mipmaps: int
//   format: int

// Struct: RenderTexture
//   id: unsigned int
//   texture: Texture
//   depth: Texture

// Struct: NPatchInfo
//   source: Rectangle
//   left: int
//   top: int
//   right: int
//   bottom: int
//   layout: int

// Struct: GlyphInfo
//   value: int
//   offsetX: int
//   offsetY: int
//   advanceX: int
//   image: Image

// Struct: Font
//   baseSize: int
//   glyphCount: int
//   glyphPadding: int
//   texture: Texture2D
//   recs: Rectangle*
//   glyphs: GlyphInfo*

// Struct: Camera3D
//   position: Vector3
//   target: Vector3
//   up: Vector3
//   fovy: float
//   projection: int

// Struct: Camera2D
//   offset: Vector2
//   target: Vector2
//   rotation: float
//   zoom: float

// Struct: Mesh
//   vertexCount: int
//   triangleCount: int
//   vertices: float*
//   texcoords: float*
//   texcoords2: float*
//   normals: float*
//   tangents: float*
//   colors: unsigned char*
//   indices: unsigned short*
//   boneCount: int
//   boneIndices: unsigned char*
//   boneWeights: float*
//   animVertices: float*
//   animNormals: float*
//   vaoId: unsigned int
//   vboId: unsigned int*

// Struct: Shader
//   id: unsigned int
//   locs: int*

// Struct: MaterialMap
//   texture: Texture2D
//   color: Color
//   value: float

// Struct: Material
//   shader: Shader
//   maps: MaterialMap*
//   params: float[4]

// Struct: Transform
//   translation: Vector3
//   rotation: Quaternion
//   scale: Vector3

// Struct: BoneInfo
//   name: char[32]
//   parent: int

// Struct: ModelSkeleton
//   boneCount: int
//   bones: BoneInfo*
//   bindPose: ModelAnimPose

// Struct: Model
//   transform: Matrix
//   meshCount: int
//   materialCount: int
//   meshes: Mesh*
//   materials: Material*
//   meshMaterial: int*
//   skeleton: ModelSkeleton
//   currentPose: ModelAnimPose
//   boneMatrices: Matrix*

// Struct: ModelAnimation
//   name: char[32]
//   boneCount: int
//   keyframeCount: int
//   keyframePoses: ModelAnimPose*

// Struct: Ray
//   position: Vector3
//   direction: Vector3

// Struct: RayCollision
//   hit: bool
//   distance: float
//   point: Vector3
//   normal: Vector3

// Struct: BoundingBox
//   min: Vector3
//   max: Vector3

// Struct: Wave
//   frameCount: unsigned int
//   sampleRate: unsigned int
//   sampleSize: unsigned int
//   channels: unsigned int
//   data: void*

// Struct: AudioStream
//   buffer: rAudioBuffer*
//   processor: rAudioProcessor*
//   sampleRate: unsigned int
//   sampleSize: unsigned int
//   channels: unsigned int

// Struct: Sound
//   stream: AudioStream
//   frameCount: unsigned int

// Struct: Music
//   stream: AudioStream
//   frameCount: unsigned int
//   looping: bool
//   ctxType: int
//   ctxData: void*

// Struct: VrDeviceInfo
//   hResolution: int
//   vResolution: int
//   hScreenSize: float
//   vScreenSize: float
//   eyeToScreenDistance: float
//   lensSeparationDistance: float
//   interpupillaryDistance: float
//   lensDistortionValues: float[4]
//   chromaAbCorrection: float[4]

// Struct: VrStereoConfig
//   projection: Matrix[2]
//   viewOffset: Matrix[2]
//   leftLensCenter: float[2]
//   rightLensCenter: float[2]
//   leftScreenCenter: float[2]
//   rightScreenCenter: float[2]
//   scale: float[2]
//   scaleIn: float[2]

// Struct: FilePathList
//   count: unsigned int
//   paths: char**

// Struct: AutomationEvent
//   frame: unsigned int
//   type: unsigned int
//   params: int[4]

// Struct: AutomationEventList
//   capacity: unsigned int
//   count: unsigned int
//   events: AutomationEvent*


// ------------------------------------------------------------
//  Enums
// ------------------------------------------------------------

// Enum: bool

// Enum: ConfigFlags
let FLAG_VSYNC_HINT = 64; // ConfigFlags
let FLAG_FULLSCREEN_MODE = 2; // ConfigFlags
let FLAG_WINDOW_RESIZABLE = 4; // ConfigFlags
let FLAG_WINDOW_UNDECORATED = 8; // ConfigFlags
let FLAG_WINDOW_HIDDEN = 128; // ConfigFlags
let FLAG_WINDOW_MINIMIZED = 512; // ConfigFlags
let FLAG_WINDOW_MAXIMIZED = 1024; // ConfigFlags
let FLAG_WINDOW_UNFOCUSED = 2048; // ConfigFlags
let FLAG_WINDOW_TOPMOST = 4096; // ConfigFlags
let FLAG_WINDOW_ALWAYS_RUN = 256; // ConfigFlags
let FLAG_WINDOW_TRANSPARENT = 16; // ConfigFlags
let FLAG_WINDOW_HIGHDPI = 8192; // ConfigFlags
let FLAG_WINDOW_MOUSE_PASSTHROUGH = 16384; // ConfigFlags
let FLAG_BORDERLESS_WINDOWED_MODE = 32768; // ConfigFlags
let FLAG_MSAA_4X_HINT = 32; // ConfigFlags
let FLAG_INTERLACED_HINT = 65536; // ConfigFlags

// Enum: TraceLogLevel
let LOG_ALL = 0; // TraceLogLevel
let LOG_TRACE = 1; // TraceLogLevel
let LOG_DEBUG = 2; // TraceLogLevel
let LOG_INFO = 3; // TraceLogLevel
let LOG_WARNING = 4; // TraceLogLevel
let LOG_ERROR = 5; // TraceLogLevel
let LOG_FATAL = 6; // TraceLogLevel
let LOG_NONE = 7; // TraceLogLevel

// Enum: KeyboardKey
let KEY_NULL = 0; // KeyboardKey
let KEY_APOSTROPHE = 39; // KeyboardKey
let KEY_COMMA = 44; // KeyboardKey
let KEY_MINUS = 45; // KeyboardKey
let KEY_PERIOD = 46; // KeyboardKey
let KEY_SLASH = 47; // KeyboardKey
let KEY_ZERO = 48; // KeyboardKey
let KEY_ONE = 49; // KeyboardKey
let KEY_TWO = 50; // KeyboardKey
let KEY_THREE = 51; // KeyboardKey
let KEY_FOUR = 52; // KeyboardKey
let KEY_FIVE = 53; // KeyboardKey
let KEY_SIX = 54; // KeyboardKey
let KEY_SEVEN = 55; // KeyboardKey
let KEY_EIGHT = 56; // KeyboardKey
let KEY_NINE = 57; // KeyboardKey
let KEY_SEMICOLON = 59; // KeyboardKey
let KEY_EQUAL = 61; // KeyboardKey
let KEY_A = 65; // KeyboardKey
let KEY_B = 66; // KeyboardKey
let KEY_C = 67; // KeyboardKey
let KEY_D = 68; // KeyboardKey
let KEY_E = 69; // KeyboardKey
let KEY_F = 70; // KeyboardKey
let KEY_G = 71; // KeyboardKey
let KEY_H = 72; // KeyboardKey
let KEY_I = 73; // KeyboardKey
let KEY_J = 74; // KeyboardKey
let KEY_K = 75; // KeyboardKey
let KEY_L = 76; // KeyboardKey
let KEY_M = 77; // KeyboardKey
let KEY_N = 78; // KeyboardKey
let KEY_O = 79; // KeyboardKey
let KEY_P = 80; // KeyboardKey
let KEY_Q = 81; // KeyboardKey
let KEY_R = 82; // KeyboardKey
let KEY_S = 83; // KeyboardKey
let KEY_T = 84; // KeyboardKey
let KEY_U = 85; // KeyboardKey
let KEY_V = 86; // KeyboardKey
let KEY_W = 87; // KeyboardKey
let KEY_X = 88; // KeyboardKey
let KEY_Y = 89; // KeyboardKey
let KEY_Z = 90; // KeyboardKey
let KEY_LEFT_BRACKET = 91; // KeyboardKey
let KEY_BACKSLASH = 92; // KeyboardKey
let KEY_RIGHT_BRACKET = 93; // KeyboardKey
let KEY_GRAVE = 96; // KeyboardKey
let KEY_SPACE = 32; // KeyboardKey
let KEY_ESCAPE = 256; // KeyboardKey
let KEY_ENTER = 257; // KeyboardKey
let KEY_TAB = 258; // KeyboardKey
let KEY_BACKSPACE = 259; // KeyboardKey
let KEY_INSERT = 260; // KeyboardKey
let KEY_DELETE = 261; // KeyboardKey
let KEY_RIGHT = 262; // KeyboardKey
let KEY_LEFT = 263; // KeyboardKey
let KEY_DOWN = 264; // KeyboardKey
let KEY_UP = 265; // KeyboardKey
let KEY_PAGE_UP = 266; // KeyboardKey
let KEY_PAGE_DOWN = 267; // KeyboardKey
let KEY_HOME = 268; // KeyboardKey
let KEY_END = 269; // KeyboardKey
let KEY_CAPS_LOCK = 280; // KeyboardKey
let KEY_SCROLL_LOCK = 281; // KeyboardKey
let KEY_NUM_LOCK = 282; // KeyboardKey
let KEY_PRINT_SCREEN = 283; // KeyboardKey
let KEY_PAUSE = 284; // KeyboardKey
let KEY_F1 = 290; // KeyboardKey
let KEY_F2 = 291; // KeyboardKey
let KEY_F3 = 292; // KeyboardKey
let KEY_F4 = 293; // KeyboardKey
let KEY_F5 = 294; // KeyboardKey
let KEY_F6 = 295; // KeyboardKey
let KEY_F7 = 296; // KeyboardKey
let KEY_F8 = 297; // KeyboardKey
let KEY_F9 = 298; // KeyboardKey
let KEY_F10 = 299; // KeyboardKey
let KEY_F11 = 300; // KeyboardKey
let KEY_F12 = 301; // KeyboardKey
let KEY_LEFT_SHIFT = 340; // KeyboardKey
let KEY_LEFT_CONTROL = 341; // KeyboardKey
let KEY_LEFT_ALT = 342; // KeyboardKey
let KEY_LEFT_SUPER = 343; // KeyboardKey
let KEY_RIGHT_SHIFT = 344; // KeyboardKey
let KEY_RIGHT_CONTROL = 345; // KeyboardKey
let KEY_RIGHT_ALT = 346; // KeyboardKey
let KEY_RIGHT_SUPER = 347; // KeyboardKey
let KEY_KB_MENU = 348; // KeyboardKey
let KEY_KP_0 = 320; // KeyboardKey
let KEY_KP_1 = 321; // KeyboardKey
let KEY_KP_2 = 322; // KeyboardKey
let KEY_KP_3 = 323; // KeyboardKey
let KEY_KP_4 = 324; // KeyboardKey
let KEY_KP_5 = 325; // KeyboardKey
let KEY_KP_6 = 326; // KeyboardKey
let KEY_KP_7 = 327; // KeyboardKey
let KEY_KP_8 = 328; // KeyboardKey
let KEY_KP_9 = 329; // KeyboardKey
let KEY_KP_DECIMAL = 330; // KeyboardKey
let KEY_KP_DIVIDE = 331; // KeyboardKey
let KEY_KP_MULTIPLY = 332; // KeyboardKey
let KEY_KP_SUBTRACT = 333; // KeyboardKey
let KEY_KP_ADD = 334; // KeyboardKey
let KEY_KP_ENTER = 335; // KeyboardKey
let KEY_KP_EQUAL = 336; // KeyboardKey
let KEY_BACK = 4; // KeyboardKey
let KEY_MENU = 5; // KeyboardKey
let KEY_VOLUME_UP = 24; // KeyboardKey
let KEY_VOLUME_DOWN = 25; // KeyboardKey

// Enum: MouseButton
let MOUSE_BUTTON_LEFT = 0; // MouseButton
let MOUSE_BUTTON_RIGHT = 1; // MouseButton
let MOUSE_BUTTON_MIDDLE = 2; // MouseButton
let MOUSE_BUTTON_SIDE = 3; // MouseButton
let MOUSE_BUTTON_EXTRA = 4; // MouseButton
let MOUSE_BUTTON_FORWARD = 5; // MouseButton
let MOUSE_BUTTON_BACK = 6; // MouseButton

// Enum: MouseCursor
let MOUSE_CURSOR_DEFAULT = 0; // MouseCursor
let MOUSE_CURSOR_ARROW = 1; // MouseCursor
let MOUSE_CURSOR_IBEAM = 2; // MouseCursor
let MOUSE_CURSOR_CROSSHAIR = 3; // MouseCursor
let MOUSE_CURSOR_POINTING_HAND = 4; // MouseCursor
let MOUSE_CURSOR_RESIZE_EW = 5; // MouseCursor
let MOUSE_CURSOR_RESIZE_NS = 6; // MouseCursor
let MOUSE_CURSOR_RESIZE_NWSE = 7; // MouseCursor
let MOUSE_CURSOR_RESIZE_NESW = 8; // MouseCursor
let MOUSE_CURSOR_RESIZE_ALL = 9; // MouseCursor
let MOUSE_CURSOR_NOT_ALLOWED = 10; // MouseCursor

// Enum: GamepadButton
let GAMEPAD_BUTTON_UNKNOWN = 0; // GamepadButton
let GAMEPAD_BUTTON_LEFT_FACE_UP = 1; // GamepadButton
let GAMEPAD_BUTTON_LEFT_FACE_RIGHT = 2; // GamepadButton
let GAMEPAD_BUTTON_LEFT_FACE_DOWN = 3; // GamepadButton
let GAMEPAD_BUTTON_LEFT_FACE_LEFT = 4; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_FACE_UP = 5; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_FACE_RIGHT = 6; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_FACE_DOWN = 7; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_FACE_LEFT = 8; // GamepadButton
let GAMEPAD_BUTTON_LEFT_TRIGGER_1 = 9; // GamepadButton
let GAMEPAD_BUTTON_LEFT_TRIGGER_2 = 10; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_TRIGGER_1 = 11; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_TRIGGER_2 = 12; // GamepadButton
let GAMEPAD_BUTTON_MIDDLE_LEFT = 13; // GamepadButton
let GAMEPAD_BUTTON_MIDDLE = 14; // GamepadButton
let GAMEPAD_BUTTON_MIDDLE_RIGHT = 15; // GamepadButton
let GAMEPAD_BUTTON_LEFT_THUMB = 16; // GamepadButton
let GAMEPAD_BUTTON_RIGHT_THUMB = 17; // GamepadButton

// Enum: GamepadAxis
let GAMEPAD_AXIS_LEFT_X = 0; // GamepadAxis
let GAMEPAD_AXIS_LEFT_Y = 1; // GamepadAxis
let GAMEPAD_AXIS_RIGHT_X = 2; // GamepadAxis
let GAMEPAD_AXIS_RIGHT_Y = 3; // GamepadAxis
let GAMEPAD_AXIS_LEFT_TRIGGER = 4; // GamepadAxis
let GAMEPAD_AXIS_RIGHT_TRIGGER = 5; // GamepadAxis

// Enum: MaterialMapIndex
let MATERIAL_MAP_ALBEDO = 0; // MaterialMapIndex
let MATERIAL_MAP_METALNESS = 1; // MaterialMapIndex
let MATERIAL_MAP_NORMAL = 2; // MaterialMapIndex
let MATERIAL_MAP_ROUGHNESS = 3; // MaterialMapIndex
let MATERIAL_MAP_OCCLUSION = 4; // MaterialMapIndex
let MATERIAL_MAP_EMISSION = 5; // MaterialMapIndex
let MATERIAL_MAP_HEIGHT = 6; // MaterialMapIndex
let MATERIAL_MAP_CUBEMAP = 7; // MaterialMapIndex
let MATERIAL_MAP_IRRADIANCE = 8; // MaterialMapIndex
let MATERIAL_MAP_PREFILTER = 9; // MaterialMapIndex
let MATERIAL_MAP_BRDF = 10; // MaterialMapIndex

// Enum: ShaderLocationIndex
let SHADER_LOC_VERTEX_POSITION = 0; // ShaderLocationIndex
let SHADER_LOC_VERTEX_TEXCOORD01 = 1; // ShaderLocationIndex
let SHADER_LOC_VERTEX_TEXCOORD02 = 2; // ShaderLocationIndex
let SHADER_LOC_VERTEX_NORMAL = 3; // ShaderLocationIndex
let SHADER_LOC_VERTEX_TANGENT = 4; // ShaderLocationIndex
let SHADER_LOC_VERTEX_COLOR = 5; // ShaderLocationIndex
let SHADER_LOC_MATRIX_MVP = 6; // ShaderLocationIndex
let SHADER_LOC_MATRIX_VIEW = 7; // ShaderLocationIndex
let SHADER_LOC_MATRIX_PROJECTION = 8; // ShaderLocationIndex
let SHADER_LOC_MATRIX_MODEL = 9; // ShaderLocationIndex
let SHADER_LOC_MATRIX_NORMAL = 10; // ShaderLocationIndex
let SHADER_LOC_VECTOR_VIEW = 11; // ShaderLocationIndex
let SHADER_LOC_COLOR_DIFFUSE = 12; // ShaderLocationIndex
let SHADER_LOC_COLOR_SPECULAR = 13; // ShaderLocationIndex
let SHADER_LOC_COLOR_AMBIENT = 14; // ShaderLocationIndex
let SHADER_LOC_MAP_ALBEDO = 15; // ShaderLocationIndex
let SHADER_LOC_MAP_METALNESS = 16; // ShaderLocationIndex
let SHADER_LOC_MAP_NORMAL = 17; // ShaderLocationIndex
let SHADER_LOC_MAP_ROUGHNESS = 18; // ShaderLocationIndex
let SHADER_LOC_MAP_OCCLUSION = 19; // ShaderLocationIndex
let SHADER_LOC_MAP_EMISSION = 20; // ShaderLocationIndex
let SHADER_LOC_MAP_HEIGHT = 21; // ShaderLocationIndex
let SHADER_LOC_MAP_CUBEMAP = 22; // ShaderLocationIndex
let SHADER_LOC_MAP_IRRADIANCE = 23; // ShaderLocationIndex
let SHADER_LOC_MAP_PREFILTER = 24; // ShaderLocationIndex
let SHADER_LOC_MAP_BRDF = 25; // ShaderLocationIndex
let SHADER_LOC_VERTEX_BONEIDS = 26; // ShaderLocationIndex
let SHADER_LOC_VERTEX_BONEWEIGHTS = 27; // ShaderLocationIndex
let SHADER_LOC_MATRIX_BONETRANSFORMS = 28; // ShaderLocationIndex
let SHADER_LOC_VERTEX_INSTANCETRANSFORM = 29; // ShaderLocationIndex

// Enum: ShaderUniformDataType
let SHADER_UNIFORM_FLOAT = 0; // ShaderUniformDataType
let SHADER_UNIFORM_VEC2 = 1; // ShaderUniformDataType
let SHADER_UNIFORM_VEC3 = 2; // ShaderUniformDataType
let SHADER_UNIFORM_VEC4 = 3; // ShaderUniformDataType
let SHADER_UNIFORM_INT = 4; // ShaderUniformDataType
let SHADER_UNIFORM_IVEC2 = 5; // ShaderUniformDataType
let SHADER_UNIFORM_IVEC3 = 6; // ShaderUniformDataType
let SHADER_UNIFORM_IVEC4 = 7; // ShaderUniformDataType
let SHADER_UNIFORM_UINT = 8; // ShaderUniformDataType
let SHADER_UNIFORM_UIVEC2 = 9; // ShaderUniformDataType
let SHADER_UNIFORM_UIVEC3 = 10; // ShaderUniformDataType
let SHADER_UNIFORM_UIVEC4 = 11; // ShaderUniformDataType
let SHADER_UNIFORM_SAMPLER2D = 12; // ShaderUniformDataType

// Enum: ShaderAttributeDataType
let SHADER_ATTRIB_FLOAT = 0; // ShaderAttributeDataType
let SHADER_ATTRIB_VEC2 = 1; // ShaderAttributeDataType
let SHADER_ATTRIB_VEC3 = 2; // ShaderAttributeDataType
let SHADER_ATTRIB_VEC4 = 3; // ShaderAttributeDataType

// Enum: PixelFormat
let PIXELFORMAT_UNCOMPRESSED_GRAYSCALE = 1; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_GRAY_ALPHA = 2; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R5G6B5 = 3; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R8G8B8 = 4; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R5G5B5A1 = 5; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R4G4B4A4 = 6; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R8G8B8A8 = 7; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R32 = 8; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R32G32B32 = 9; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R32G32B32A32 = 10; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R16 = 11; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R16G16B16 = 12; // PixelFormat
let PIXELFORMAT_UNCOMPRESSED_R16G16B16A16 = 13; // PixelFormat
let PIXELFORMAT_COMPRESSED_DXT1_RGB = 14; // PixelFormat
let PIXELFORMAT_COMPRESSED_DXT1_RGBA = 15; // PixelFormat
let PIXELFORMAT_COMPRESSED_DXT3_RGBA = 16; // PixelFormat
let PIXELFORMAT_COMPRESSED_DXT5_RGBA = 17; // PixelFormat
let PIXELFORMAT_COMPRESSED_ETC1_RGB = 18; // PixelFormat
let PIXELFORMAT_COMPRESSED_ETC2_RGB = 19; // PixelFormat
let PIXELFORMAT_COMPRESSED_ETC2_EAC_RGBA = 20; // PixelFormat
let PIXELFORMAT_COMPRESSED_PVRT_RGB = 21; // PixelFormat
let PIXELFORMAT_COMPRESSED_PVRT_RGBA = 22; // PixelFormat
let PIXELFORMAT_COMPRESSED_ASTC_4x4_RGBA = 23; // PixelFormat
let PIXELFORMAT_COMPRESSED_ASTC_8x8_RGBA = 24; // PixelFormat

// Enum: TextureFilter
let TEXTURE_FILTER_POINT = 0; // TextureFilter
let TEXTURE_FILTER_BILINEAR = 1; // TextureFilter
let TEXTURE_FILTER_TRILINEAR = 2; // TextureFilter
let TEXTURE_FILTER_ANISOTROPIC_4X = 3; // TextureFilter
let TEXTURE_FILTER_ANISOTROPIC_8X = 4; // TextureFilter
let TEXTURE_FILTER_ANISOTROPIC_16X = 5; // TextureFilter

// Enum: TextureWrap
let TEXTURE_WRAP_REPEAT = 0; // TextureWrap
let TEXTURE_WRAP_CLAMP = 1; // TextureWrap
let TEXTURE_WRAP_MIRROR_REPEAT = 2; // TextureWrap
let TEXTURE_WRAP_MIRROR_CLAMP = 3; // TextureWrap

// Enum: CubemapLayout
let CUBEMAP_LAYOUT_AUTO_DETECT = 0; // CubemapLayout
let CUBEMAP_LAYOUT_LINE_VERTICAL = 1; // CubemapLayout
let CUBEMAP_LAYOUT_LINE_HORIZONTAL = 2; // CubemapLayout
let CUBEMAP_LAYOUT_CROSS_THREE_BY_FOUR = 3; // CubemapLayout
let CUBEMAP_LAYOUT_CROSS_FOUR_BY_THREE = 4; // CubemapLayout

// Enum: FontType
let FONT_DEFAULT = 0; // FontType
let FONT_BITMAP = 1; // FontType
let FONT_SDF = 2; // FontType

// Enum: BlendMode
let BLEND_ALPHA = 0; // BlendMode
let BLEND_ADDITIVE = 1; // BlendMode
let BLEND_MULTIPLIED = 2; // BlendMode
let BLEND_ADD_COLORS = 3; // BlendMode
let BLEND_SUBTRACT_COLORS = 4; // BlendMode
let BLEND_ALPHA_PREMULTIPLY = 5; // BlendMode
let BLEND_CUSTOM = 6; // BlendMode
let BLEND_CUSTOM_SEPARATE = 7; // BlendMode

// Enum: Gesture
let GESTURE_NONE = 0; // Gesture
let GESTURE_TAP = 1; // Gesture
let GESTURE_DOUBLETAP = 2; // Gesture
let GESTURE_HOLD = 4; // Gesture
let GESTURE_DRAG = 8; // Gesture
let GESTURE_SWIPE_RIGHT = 16; // Gesture
let GESTURE_SWIPE_LEFT = 32; // Gesture
let GESTURE_SWIPE_UP = 64; // Gesture
let GESTURE_SWIPE_DOWN = 128; // Gesture
let GESTURE_PINCH_IN = 256; // Gesture
let GESTURE_PINCH_OUT = 512; // Gesture

// Enum: CameraMode
let CAMERA_CUSTOM = 0; // CameraMode
let CAMERA_FREE = 1; // CameraMode
let CAMERA_ORBITAL = 2; // CameraMode
let CAMERA_FIRST_PERSON = 3; // CameraMode
let CAMERA_THIRD_PERSON = 4; // CameraMode

// Enum: CameraProjection
let CAMERA_PERSPECTIVE = 0; // CameraProjection
let CAMERA_ORTHOGRAPHIC = 1; // CameraProjection

// Enum: NPatchLayout
let NPATCH_NINE_PATCH = 0; // NPatchLayout
let NPATCH_THREE_PATCH_VERTICAL = 1; // NPatchLayout
let NPATCH_THREE_PATCH_HORIZONTAL = 2; // NPatchLayout


// ------------------------------------------------------------
//  Audio
// ------------------------------------------------------------

/// Initialises audio device.
// koda:extern InitAudioDevice koda_wrap_raylib_InitAudioDevice 0
let InitAudioDevice = 0;

/// Pauses audio stream.
/// @param stream  object  — boxed object (struct layout depends on the C API)
// koda:extern PauseAudioStream koda_wrap_raylib_PauseAudioStream 1
let PauseAudioStream = 0;

/// Pauses music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
// koda:extern PauseMusicStream koda_wrap_raylib_PauseMusicStream 1
let PauseMusicStream = 0;

/// Pauses sound.
/// @param sound  object  — boxed object (struct layout depends on the C API)
// koda:extern PauseSound koda_wrap_raylib_PauseSound 1
let PauseSound = 0;

/// Plays audio stream.
/// @param stream  object  — boxed object (struct layout depends on the C API)
// koda:extern PlayAudioStream koda_wrap_raylib_PlayAudioStream 1
let PlayAudioStream = 0;

/// Plays automation event.
/// @param event  object  — boxed object (struct layout depends on the C API)
// koda:extern PlayAutomationEvent koda_wrap_raylib_PlayAutomationEvent 1
let PlayAutomationEvent = 0;

/// Plays music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
// koda:extern PlayMusicStream koda_wrap_raylib_PlayMusicStream 1
let PlayMusicStream = 0;

/// Plays sound.
/// @param sound  object  — boxed object (struct layout depends on the C API)
// koda:extern PlaySound koda_wrap_raylib_PlaySound 1
let PlaySound = 0;

/// Stops audio stream.
/// @param stream  object  — boxed object (struct layout depends on the C API)
// koda:extern StopAudioStream koda_wrap_raylib_StopAudioStream 1
let StopAudioStream = 0;

/// Stops automation event recording.
// koda:extern StopAutomationEventRecording koda_wrap_raylib_StopAutomationEventRecording 0
let StopAutomationEventRecording = 0;

/// Stops music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
// koda:extern StopMusicStream koda_wrap_raylib_StopMusicStream 1
let StopMusicStream = 0;

/// Stops sound.
/// @param sound  object  — boxed object (struct layout depends on the C API)
// koda:extern StopSound koda_wrap_raylib_StopSound 1
let StopSound = 0;

// ------------------------------------------------------------
//  Checks
// ------------------------------------------------------------

/// Checks collision box sphere.
/// @param box  object  — boxed object (struct layout depends on the C API)
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @returns bool — result from the native call
// koda:extern CheckCollisionBoxSphere koda_wrap_raylib_CheckCollisionBoxSphere 3
let CheckCollisionBoxSphere = 0;

/// Checks collision boxes.
/// @param box1  object  — boxed object (struct layout depends on the C API)
/// @param box2  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionBoxes koda_wrap_raylib_CheckCollisionBoxes 2
let CheckCollisionBoxes = 0;

/// Checks collision circle line.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionCircleLine koda_wrap_raylib_CheckCollisionCircleLine 4
let CheckCollisionCircleLine = 0;

/// Checks collision circle rec.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionCircleRec koda_wrap_raylib_CheckCollisionCircleRec 3
let CheckCollisionCircleRec = 0;

/// Checks collision circles.
/// @param center1  object  — boxed object (struct layout depends on the C API)
/// @param radius1  number  — numeric value
/// @param center2  object  — boxed object (struct layout depends on the C API)
/// @param radius2  number  — numeric value
/// @returns bool — result from the native call
// koda:extern CheckCollisionCircles koda_wrap_raylib_CheckCollisionCircles 4
let CheckCollisionCircles = 0;

/// Checks collision lines.
/// @param startPos1  object  — boxed object (struct layout depends on the C API)
/// @param endPos1  object  — boxed object (struct layout depends on the C API)
/// @param startPos2  object  — boxed object (struct layout depends on the C API)
/// @param endPos2  object  — boxed object (struct layout depends on the C API)
/// @param collisionPoint  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionLines koda_wrap_raylib_CheckCollisionLines 5
let CheckCollisionLines = 0;

/// Checks collision point circle.
/// @param point  object  — boxed object (struct layout depends on the C API)
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @returns bool — result from the native call
// koda:extern CheckCollisionPointCircle koda_wrap_raylib_CheckCollisionPointCircle 3
let CheckCollisionPointCircle = 0;

/// Checks collision point line.
/// @param point  object  — boxed object (struct layout depends on the C API)
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param threshold  number  — numeric value
/// @returns bool — result from the native call
// koda:extern CheckCollisionPointLine koda_wrap_raylib_CheckCollisionPointLine 4
let CheckCollisionPointLine = 0;

/// Checks collision point poly.
/// @param point  object  — boxed object (struct layout depends on the C API)
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @returns bool — result from the native call
// koda:extern CheckCollisionPointPoly koda_wrap_raylib_CheckCollisionPointPoly 3
let CheckCollisionPointPoly = 0;

/// Checks collision point rec.
/// @param point  object  — boxed object (struct layout depends on the C API)
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionPointRec koda_wrap_raylib_CheckCollisionPointRec 2
let CheckCollisionPointRec = 0;

/// Checks collision point triangle.
/// @param point  object  — boxed object (struct layout depends on the C API)
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionPointTriangle koda_wrap_raylib_CheckCollisionPointTriangle 4
let CheckCollisionPointTriangle = 0;

/// Checks collision recs.
/// @param rec1  object  — boxed object (struct layout depends on the C API)
/// @param rec2  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern CheckCollisionRecs koda_wrap_raylib_CheckCollisionRecs 2
let CheckCollisionRecs = 0;

/// Checks collision spheres.
/// @param center1  object  — boxed object (struct layout depends on the C API)
/// @param radius1  number  — numeric value
/// @param center2  object  — boxed object (struct layout depends on the C API)
/// @param radius2  number  — numeric value
/// @returns bool — result from the native call
// koda:extern CheckCollisionSpheres koda_wrap_raylib_CheckCollisionSpheres 4
let CheckCollisionSpheres = 0;

/// Returns true if audio device ready.
/// @returns bool — result from the native call
// koda:extern IsAudioDeviceReady koda_wrap_raylib_IsAudioDeviceReady 0
let IsAudioDeviceReady = 0;

/// Returns true if audio stream playing.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsAudioStreamPlaying koda_wrap_raylib_IsAudioStreamPlaying 1
let IsAudioStreamPlaying = 0;

/// Returns true if audio stream processed.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsAudioStreamProcessed koda_wrap_raylib_IsAudioStreamProcessed 1
let IsAudioStreamProcessed = 0;

/// Returns true if audio stream valid.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsAudioStreamValid koda_wrap_raylib_IsAudioStreamValid 1
let IsAudioStreamValid = 0;

/// Returns true if cursor hidden.
/// @returns bool — result from the native call
// koda:extern IsCursorHidden koda_wrap_raylib_IsCursorHidden 0
let IsCursorHidden = 0;

/// Returns true if cursor on screen.
/// @returns bool — result from the native call
// koda:extern IsCursorOnScreen koda_wrap_raylib_IsCursorOnScreen 0
let IsCursorOnScreen = 0;

/// Returns true if file dropped.
/// @returns bool — result from the native call
// koda:extern IsFileDropped koda_wrap_raylib_IsFileDropped 0
let IsFileDropped = 0;

/// Returns true if file extension.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param ext  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern IsFileExtension koda_wrap_raylib_IsFileExtension 2
let IsFileExtension = 0;

/// Returns true if file name valid.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern IsFileNameValid koda_wrap_raylib_IsFileNameValid 1
let IsFileNameValid = 0;

/// Returns true if font valid.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsFontValid koda_wrap_raylib_IsFontValid 1
let IsFontValid = 0;

/// Returns true if gamepad available.
/// @param gamepad  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsGamepadAvailable koda_wrap_raylib_IsGamepadAvailable 1
let IsGamepadAvailable = 0;

/// Returns true if gamepad button down.
/// @param gamepad  number  — numeric value
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsGamepadButtonDown koda_wrap_raylib_IsGamepadButtonDown 2
let IsGamepadButtonDown = 0;

/// Returns true if gamepad button pressed.
/// @param gamepad  number  — numeric value
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsGamepadButtonPressed koda_wrap_raylib_IsGamepadButtonPressed 2
let IsGamepadButtonPressed = 0;

/// Returns true if gamepad button released.
/// @param gamepad  number  — numeric value
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsGamepadButtonReleased koda_wrap_raylib_IsGamepadButtonReleased 2
let IsGamepadButtonReleased = 0;

/// Returns true if gamepad button up.
/// @param gamepad  number  — numeric value
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsGamepadButtonUp koda_wrap_raylib_IsGamepadButtonUp 2
let IsGamepadButtonUp = 0;

/// Returns true if gesture detected.
/// @param gesture  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsGestureDetected koda_wrap_raylib_IsGestureDetected 1
let IsGestureDetected = 0;

/// Returns true if image valid.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsImageValid koda_wrap_raylib_IsImageValid 1
let IsImageValid = 0;

/// Returns true if key down.
/// @param key  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsKeyDown koda_wrap_raylib_IsKeyDown 1
let IsKeyDown = 0;

/// Returns true if key pressed.
/// @param key  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsKeyPressed koda_wrap_raylib_IsKeyPressed 1
let IsKeyPressed = 0;

/// Returns true if key pressed repeat.
/// @param key  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsKeyPressedRepeat koda_wrap_raylib_IsKeyPressedRepeat 1
let IsKeyPressedRepeat = 0;

/// Returns true if key released.
/// @param key  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsKeyReleased koda_wrap_raylib_IsKeyReleased 1
let IsKeyReleased = 0;

/// Returns true if key up.
/// @param key  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsKeyUp koda_wrap_raylib_IsKeyUp 1
let IsKeyUp = 0;

/// Returns true if material valid.
/// @param material  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsMaterialValid koda_wrap_raylib_IsMaterialValid 1
let IsMaterialValid = 0;

/// Returns true if model animation valid.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param anim  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsModelAnimationValid koda_wrap_raylib_IsModelAnimationValid 2
let IsModelAnimationValid = 0;

/// Returns true if model valid.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsModelValid koda_wrap_raylib_IsModelValid 1
let IsModelValid = 0;

/// Returns true if mouse button down.
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsMouseButtonDown koda_wrap_raylib_IsMouseButtonDown 1
let IsMouseButtonDown = 0;

/// Returns true if mouse button pressed.
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsMouseButtonPressed koda_wrap_raylib_IsMouseButtonPressed 1
let IsMouseButtonPressed = 0;

/// Returns true if mouse button released.
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsMouseButtonReleased koda_wrap_raylib_IsMouseButtonReleased 1
let IsMouseButtonReleased = 0;

/// Returns true if mouse button up.
/// @param button  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsMouseButtonUp koda_wrap_raylib_IsMouseButtonUp 1
let IsMouseButtonUp = 0;

/// Returns true if music stream playing.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsMusicStreamPlaying koda_wrap_raylib_IsMusicStreamPlaying 1
let IsMusicStreamPlaying = 0;

/// Returns true if music valid.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsMusicValid koda_wrap_raylib_IsMusicValid 1
let IsMusicValid = 0;

/// Returns true if path file.
/// @param path  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern IsPathFile koda_wrap_raylib_IsPathFile 1
let IsPathFile = 0;

/// Returns true if render texture valid.
/// @param target  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsRenderTextureValid koda_wrap_raylib_IsRenderTextureValid 1
let IsRenderTextureValid = 0;

/// Returns true if shader valid.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsShaderValid koda_wrap_raylib_IsShaderValid 1
let IsShaderValid = 0;

/// Returns true if sound playing.
/// @param sound  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsSoundPlaying koda_wrap_raylib_IsSoundPlaying 1
let IsSoundPlaying = 0;

/// Returns true if sound valid.
/// @param sound  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsSoundValid koda_wrap_raylib_IsSoundValid 1
let IsSoundValid = 0;

/// Returns true if texture valid.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsTextureValid koda_wrap_raylib_IsTextureValid 1
let IsTextureValid = 0;

/// Returns true if wave valid.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern IsWaveValid koda_wrap_raylib_IsWaveValid 1
let IsWaveValid = 0;

/// Returns true if window focused.
/// @returns bool — result from the native call
// koda:extern IsWindowFocused koda_wrap_raylib_IsWindowFocused 0
let IsWindowFocused = 0;

/// Returns true if window fullscreen.
/// @returns bool — result from the native call
// koda:extern IsWindowFullscreen koda_wrap_raylib_IsWindowFullscreen 0
let IsWindowFullscreen = 0;

/// Returns true if window hidden.
/// @returns bool — result from the native call
// koda:extern IsWindowHidden koda_wrap_raylib_IsWindowHidden 0
let IsWindowHidden = 0;

/// Returns true if window maximized.
/// @returns bool — result from the native call
// koda:extern IsWindowMaximized koda_wrap_raylib_IsWindowMaximized 0
let IsWindowMaximized = 0;

/// Returns true if window minimized.
/// @returns bool — result from the native call
// koda:extern IsWindowMinimized koda_wrap_raylib_IsWindowMinimized 0
let IsWindowMinimized = 0;

/// Returns true if window ready.
/// @returns bool — result from the native call
// koda:extern IsWindowReady koda_wrap_raylib_IsWindowReady 0
let IsWindowReady = 0;

/// Returns true if window resized.
/// @returns bool — result from the native call
// koda:extern IsWindowResized koda_wrap_raylib_IsWindowResized 0
let IsWindowResized = 0;

/// Returns true if window state.
/// @param flag  number  — numeric value
/// @returns bool — result from the native call
// koda:extern IsWindowState koda_wrap_raylib_IsWindowState 1
let IsWindowState = 0;

// ------------------------------------------------------------
//  Cleanup
// ------------------------------------------------------------

/// Closes audio device.
// koda:extern CloseAudioDevice koda_wrap_raylib_CloseAudioDevice 0
let CloseAudioDevice = 0;

/// Closes window.
// koda:extern CloseWindow koda_wrap_raylib_CloseWindow 0
let CloseWindow = 0;

// ------------------------------------------------------------
//  Configuration
// ------------------------------------------------------------

/// Disables cursor.
// koda:extern DisableCursor koda_wrap_raylib_DisableCursor 0
let DisableCursor = 0;

/// Disables event waiting.
// koda:extern DisableEventWaiting koda_wrap_raylib_DisableEventWaiting 0
let DisableEventWaiting = 0;

/// Enables cursor.
// koda:extern EnableCursor koda_wrap_raylib_EnableCursor 0
let EnableCursor = 0;

/// Enables event waiting.
// koda:extern EnableEventWaiting koda_wrap_raylib_EnableEventWaiting 0
let EnableEventWaiting = 0;

// ------------------------------------------------------------
//  Debug
// ------------------------------------------------------------

/// Calls log.
/// @param logLevel  number  — numeric value
/// @param text  string  — null-terminated C string passed as Koda string
// koda:extern TraceLog koda_wrap_raylib_TraceLog 2
let TraceLog = 0;

// ------------------------------------------------------------
//  Drawing
// ------------------------------------------------------------

/// Draws billboard.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param scale  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawBillboard koda_wrap_raylib_DrawBillboard 5
let DrawBillboard = 0;

/// Draws billboard pro.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param source  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param up  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param origin  object  — boxed object (struct layout depends on the C API)
/// @param rotation  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawBillboardPro koda_wrap_raylib_DrawBillboardPro 9
let DrawBillboardPro = 0;

/// Draws billboard rec.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param source  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawBillboardRec koda_wrap_raylib_DrawBillboardRec 6
let DrawBillboardRec = 0;

/// Draws bounding box.
/// @param box  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawBoundingBox koda_wrap_raylib_DrawBoundingBox 2
let DrawBoundingBox = 0;

/// Draws capsule.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param slices  number  — numeric value
/// @param rings  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCapsule koda_wrap_raylib_DrawCapsule 6
let DrawCapsule = 0;

/// Draws capsule wires.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param slices  number  — numeric value
/// @param rings  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCapsuleWires koda_wrap_raylib_DrawCapsuleWires 6
let DrawCapsuleWires = 0;

/// Draws circle.
/// @param centerX  number  — numeric value
/// @param centerY  number  — numeric value
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircle koda_wrap_raylib_DrawCircle 4
let DrawCircle = 0;

/// Draws circle3 d.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param rotationAxis  object  — boxed object (struct layout depends on the C API)
/// @param rotationAngle  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircle3D koda_wrap_raylib_DrawCircle3D 5
let DrawCircle3D = 0;

/// Draws circle gradient.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param inner  object  — boxed object (struct layout depends on the C API)
/// @param outer  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircleGradient koda_wrap_raylib_DrawCircleGradient 4
let DrawCircleGradient = 0;

/// Draws circle lines.
/// @param centerX  number  — numeric value
/// @param centerY  number  — numeric value
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircleLines koda_wrap_raylib_DrawCircleLines 4
let DrawCircleLines = 0;

/// Draws circle lines v.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircleLinesV koda_wrap_raylib_DrawCircleLinesV 3
let DrawCircleLinesV = 0;

/// Draws circle sector.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param startAngle  number  — numeric value
/// @param endAngle  number  — numeric value
/// @param segments  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircleSector koda_wrap_raylib_DrawCircleSector 6
let DrawCircleSector = 0;

/// Draws circle sector lines.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param startAngle  number  — numeric value
/// @param endAngle  number  — numeric value
/// @param segments  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircleSectorLines koda_wrap_raylib_DrawCircleSectorLines 6
let DrawCircleSectorLines = 0;

/// Draws circle v.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCircleV koda_wrap_raylib_DrawCircleV 3
let DrawCircleV = 0;

/// Draws cube.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param length  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCube koda_wrap_raylib_DrawCube 5
let DrawCube = 0;

/// Draws cube v.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCubeV koda_wrap_raylib_DrawCubeV 3
let DrawCubeV = 0;

/// Draws cube wires.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param length  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCubeWires koda_wrap_raylib_DrawCubeWires 5
let DrawCubeWires = 0;

/// Draws cube wires v.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCubeWiresV koda_wrap_raylib_DrawCubeWiresV 3
let DrawCubeWiresV = 0;

/// Draws cylinder.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param radiusTop  number  — numeric value
/// @param radiusBottom  number  — numeric value
/// @param height  number  — numeric value
/// @param slices  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCylinder koda_wrap_raylib_DrawCylinder 6
let DrawCylinder = 0;

/// Draws cylinder ex.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param startRadius  number  — numeric value
/// @param endRadius  number  — numeric value
/// @param sides  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCylinderEx koda_wrap_raylib_DrawCylinderEx 6
let DrawCylinderEx = 0;

/// Draws cylinder wires.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param radiusTop  number  — numeric value
/// @param radiusBottom  number  — numeric value
/// @param height  number  — numeric value
/// @param slices  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCylinderWires koda_wrap_raylib_DrawCylinderWires 6
let DrawCylinderWires = 0;

/// Draws cylinder wires ex.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param startRadius  number  — numeric value
/// @param endRadius  number  — numeric value
/// @param sides  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawCylinderWiresEx koda_wrap_raylib_DrawCylinderWiresEx 6
let DrawCylinderWiresEx = 0;

/// Draws ellipse.
/// @param centerX  number  — numeric value
/// @param centerY  number  — numeric value
/// @param radiusH  number  — numeric value
/// @param radiusV  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawEllipse koda_wrap_raylib_DrawEllipse 5
let DrawEllipse = 0;

/// Draws ellipse lines.
/// @param centerX  number  — numeric value
/// @param centerY  number  — numeric value
/// @param radiusH  number  — numeric value
/// @param radiusV  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawEllipseLines koda_wrap_raylib_DrawEllipseLines 5
let DrawEllipseLines = 0;

/// Draws ellipse lines v.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radiusH  number  — numeric value
/// @param radiusV  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawEllipseLinesV koda_wrap_raylib_DrawEllipseLinesV 4
let DrawEllipseLinesV = 0;

/// Draws ellipse v.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radiusH  number  — numeric value
/// @param radiusV  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawEllipseV koda_wrap_raylib_DrawEllipseV 4
let DrawEllipseV = 0;

/// Draws f p s.
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
// koda:extern DrawFPS koda_wrap_raylib_DrawFPS 2
let DrawFPS = 0;

/// Draws grid.
/// @param slices  number  — numeric value
/// @param spacing  number  — numeric value
// koda:extern DrawGrid koda_wrap_raylib_DrawGrid 2
let DrawGrid = 0;

/// Draws line.
/// @param startPosX  number  — numeric value
/// @param startPosY  number  — numeric value
/// @param endPosX  number  — numeric value
/// @param endPosY  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLine koda_wrap_raylib_DrawLine 5
let DrawLine = 0;

/// Draws line3 d.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLine3D koda_wrap_raylib_DrawLine3D 3
let DrawLine3D = 0;

/// Draws line bezier.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLineBezier koda_wrap_raylib_DrawLineBezier 4
let DrawLineBezier = 0;

/// Draws line dashed.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param dashSize  number  — numeric value
/// @param spaceSize  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLineDashed koda_wrap_raylib_DrawLineDashed 5
let DrawLineDashed = 0;

/// Draws line ex.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLineEx koda_wrap_raylib_DrawLineEx 4
let DrawLineEx = 0;

/// Draws line strip.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLineStrip koda_wrap_raylib_DrawLineStrip 3
let DrawLineStrip = 0;

/// Draws line v.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawLineV koda_wrap_raylib_DrawLineV 3
let DrawLineV = 0;

/// Draws mesh.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param material  object  — boxed object (struct layout depends on the C API)
/// @param transform  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawMesh koda_wrap_raylib_DrawMesh 3
let DrawMesh = 0;

/// Draws mesh instanced.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param material  object  — boxed object (struct layout depends on the C API)
/// @param transforms  object  — boxed object (struct layout depends on the C API)
/// @param instances  number  — numeric value
// koda:extern DrawMeshInstanced koda_wrap_raylib_DrawMeshInstanced 4
let DrawMeshInstanced = 0;

/// Draws model.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param scale  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawModel koda_wrap_raylib_DrawModel 4
let DrawModel = 0;

/// Draws model ex.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param rotationAxis  object  — boxed object (struct layout depends on the C API)
/// @param rotationAngle  number  — numeric value
/// @param scale  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawModelEx koda_wrap_raylib_DrawModelEx 6
let DrawModelEx = 0;

/// Draws model wires.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param scale  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawModelWires koda_wrap_raylib_DrawModelWires 4
let DrawModelWires = 0;

/// Draws model wires ex.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param rotationAxis  object  — boxed object (struct layout depends on the C API)
/// @param rotationAngle  number  — numeric value
/// @param scale  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawModelWiresEx koda_wrap_raylib_DrawModelWiresEx 6
let DrawModelWiresEx = 0;

/// Draws pixel.
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPixel koda_wrap_raylib_DrawPixel 3
let DrawPixel = 0;

/// Draws pixel v.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPixelV koda_wrap_raylib_DrawPixelV 2
let DrawPixelV = 0;

/// Draws plane.
/// @param centerPos  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPlane koda_wrap_raylib_DrawPlane 3
let DrawPlane = 0;

/// Draws point3 d.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPoint3D koda_wrap_raylib_DrawPoint3D 2
let DrawPoint3D = 0;

/// Draws poly.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param sides  number  — numeric value
/// @param radius  number  — numeric value
/// @param rotation  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPoly koda_wrap_raylib_DrawPoly 5
let DrawPoly = 0;

/// Draws poly lines.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param sides  number  — numeric value
/// @param radius  number  — numeric value
/// @param rotation  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPolyLines koda_wrap_raylib_DrawPolyLines 5
let DrawPolyLines = 0;

/// Draws poly lines ex.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param sides  number  — numeric value
/// @param radius  number  — numeric value
/// @param rotation  number  — numeric value
/// @param lineThick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawPolyLinesEx koda_wrap_raylib_DrawPolyLinesEx 6
let DrawPolyLinesEx = 0;

/// Draws ray.
/// @param ray  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRay koda_wrap_raylib_DrawRay 2
let DrawRay = 0;

/// Draws rectangle.
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangle koda_wrap_raylib_DrawRectangle 5
let DrawRectangle = 0;

/// Draws rectangle gradient ex.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param topLeft  object  — boxed object (struct layout depends on the C API)
/// @param bottomLeft  object  — boxed object (struct layout depends on the C API)
/// @param bottomRight  object  — boxed object (struct layout depends on the C API)
/// @param topRight  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleGradientEx koda_wrap_raylib_DrawRectangleGradientEx 5
let DrawRectangleGradientEx = 0;

/// Draws rectangle gradient h.
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param left  object  — boxed object (struct layout depends on the C API)
/// @param right  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleGradientH koda_wrap_raylib_DrawRectangleGradientH 6
let DrawRectangleGradientH = 0;

/// Draws rectangle gradient v.
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param top  object  — boxed object (struct layout depends on the C API)
/// @param bottom  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleGradientV koda_wrap_raylib_DrawRectangleGradientV 6
let DrawRectangleGradientV = 0;

/// Draws rectangle lines.
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleLines koda_wrap_raylib_DrawRectangleLines 5
let DrawRectangleLines = 0;

/// Draws rectangle lines ex.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param lineThick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleLinesEx koda_wrap_raylib_DrawRectangleLinesEx 3
let DrawRectangleLinesEx = 0;

/// Draws rectangle pro.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param origin  object  — boxed object (struct layout depends on the C API)
/// @param rotation  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectanglePro koda_wrap_raylib_DrawRectanglePro 4
let DrawRectanglePro = 0;

/// Draws rectangle rec.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleRec koda_wrap_raylib_DrawRectangleRec 2
let DrawRectangleRec = 0;

/// Draws rectangle rounded.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param roundness  number  — numeric value
/// @param segments  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleRounded koda_wrap_raylib_DrawRectangleRounded 4
let DrawRectangleRounded = 0;

/// Draws rectangle rounded lines.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param roundness  number  — numeric value
/// @param segments  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleRoundedLines koda_wrap_raylib_DrawRectangleRoundedLines 4
let DrawRectangleRoundedLines = 0;

/// Draws rectangle rounded lines ex.
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param roundness  number  — numeric value
/// @param segments  number  — numeric value
/// @param lineThick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleRoundedLinesEx koda_wrap_raylib_DrawRectangleRoundedLinesEx 5
let DrawRectangleRoundedLinesEx = 0;

/// Draws rectangle v.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRectangleV koda_wrap_raylib_DrawRectangleV 3
let DrawRectangleV = 0;

/// Draws ring.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param innerRadius  number  — numeric value
/// @param outerRadius  number  — numeric value
/// @param startAngle  number  — numeric value
/// @param endAngle  number  — numeric value
/// @param segments  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRing koda_wrap_raylib_DrawRing 7
let DrawRing = 0;

/// Draws ring lines.
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param innerRadius  number  — numeric value
/// @param outerRadius  number  — numeric value
/// @param startAngle  number  — numeric value
/// @param endAngle  number  — numeric value
/// @param segments  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawRingLines koda_wrap_raylib_DrawRingLines 7
let DrawRingLines = 0;

/// Draws sphere.
/// @param centerPos  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSphere koda_wrap_raylib_DrawSphere 3
let DrawSphere = 0;

/// Draws sphere ex.
/// @param centerPos  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param rings  number  — numeric value
/// @param slices  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSphereEx koda_wrap_raylib_DrawSphereEx 5
let DrawSphereEx = 0;

/// Draws sphere wires.
/// @param centerPos  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param rings  number  — numeric value
/// @param slices  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSphereWires koda_wrap_raylib_DrawSphereWires 5
let DrawSphereWires = 0;

/// Draws spline basis.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineBasis koda_wrap_raylib_DrawSplineBasis 4
let DrawSplineBasis = 0;

/// Draws spline bezier cubic.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineBezierCubic koda_wrap_raylib_DrawSplineBezierCubic 4
let DrawSplineBezierCubic = 0;

/// Draws spline bezier quadratic.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineBezierQuadratic koda_wrap_raylib_DrawSplineBezierQuadratic 4
let DrawSplineBezierQuadratic = 0;

/// Draws spline catmull rom.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineCatmullRom koda_wrap_raylib_DrawSplineCatmullRom 4
let DrawSplineCatmullRom = 0;

/// Draws spline linear.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineLinear koda_wrap_raylib_DrawSplineLinear 4
let DrawSplineLinear = 0;

/// Draws spline segment basis.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineSegmentBasis koda_wrap_raylib_DrawSplineSegmentBasis 6
let DrawSplineSegmentBasis = 0;

/// Draws spline segment bezier cubic.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param c2  object  — boxed object (struct layout depends on the C API)
/// @param c3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineSegmentBezierCubic koda_wrap_raylib_DrawSplineSegmentBezierCubic 6
let DrawSplineSegmentBezierCubic = 0;

/// Draws spline segment bezier quadratic.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param c2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineSegmentBezierQuadratic koda_wrap_raylib_DrawSplineSegmentBezierQuadratic 5
let DrawSplineSegmentBezierQuadratic = 0;

/// Draws spline segment catmull rom.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineSegmentCatmullRom koda_wrap_raylib_DrawSplineSegmentCatmullRom 6
let DrawSplineSegmentCatmullRom = 0;

/// Draws spline segment linear.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawSplineSegmentLinear koda_wrap_raylib_DrawSplineSegmentLinear 4
let DrawSplineSegmentLinear = 0;

/// Draws text.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param fontSize  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawText koda_wrap_raylib_DrawText 5
let DrawText = 0;

/// Draws text codepoint.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param codepoint  number  — numeric value
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param fontSize  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextCodepoint koda_wrap_raylib_DrawTextCodepoint 5
let DrawTextCodepoint = 0;

/// Draws text codepoints.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param codepoints  number  — numeric value
/// @param codepointCount  number  — numeric value
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextCodepoints koda_wrap_raylib_DrawTextCodepoints 7
let DrawTextCodepoints = 0;

/// Draws text ex.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param text  string  — null-terminated C string passed as Koda string
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextEx koda_wrap_raylib_DrawTextEx 6
let DrawTextEx = 0;

/// Draws text pro.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param text  string  — null-terminated C string passed as Koda string
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param origin  object  — boxed object (struct layout depends on the C API)
/// @param rotation  number  — numeric value
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextPro koda_wrap_raylib_DrawTextPro 8
let DrawTextPro = 0;

/// Draws texture.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTexture koda_wrap_raylib_DrawTexture 4
let DrawTexture = 0;

/// Draws texture ex.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param rotation  number  — numeric value
/// @param scale  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextureEx koda_wrap_raylib_DrawTextureEx 5
let DrawTextureEx = 0;

/// Draws texture n patch.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param nPatchInfo  object  — boxed object (struct layout depends on the C API)
/// @param dest  object  — boxed object (struct layout depends on the C API)
/// @param origin  object  — boxed object (struct layout depends on the C API)
/// @param rotation  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextureNPatch koda_wrap_raylib_DrawTextureNPatch 6
let DrawTextureNPatch = 0;

/// Draws texture pro.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param source  object  — boxed object (struct layout depends on the C API)
/// @param dest  object  — boxed object (struct layout depends on the C API)
/// @param origin  object  — boxed object (struct layout depends on the C API)
/// @param rotation  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTexturePro koda_wrap_raylib_DrawTexturePro 6
let DrawTexturePro = 0;

/// Draws texture rec.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param source  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextureRec koda_wrap_raylib_DrawTextureRec 4
let DrawTextureRec = 0;

/// Draws texture v.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTextureV koda_wrap_raylib_DrawTextureV 3
let DrawTextureV = 0;

/// Draws triangle.
/// @param v1  object  — boxed object (struct layout depends on the C API)
/// @param v2  object  — boxed object (struct layout depends on the C API)
/// @param v3  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTriangle koda_wrap_raylib_DrawTriangle 4
let DrawTriangle = 0;

/// Draws triangle3 d.
/// @param v1  object  — boxed object (struct layout depends on the C API)
/// @param v2  object  — boxed object (struct layout depends on the C API)
/// @param v3  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTriangle3D koda_wrap_raylib_DrawTriangle3D 4
let DrawTriangle3D = 0;

/// Draws triangle fan.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTriangleFan koda_wrap_raylib_DrawTriangleFan 3
let DrawTriangleFan = 0;

/// Draws triangle lines.
/// @param v1  object  — boxed object (struct layout depends on the C API)
/// @param v2  object  — boxed object (struct layout depends on the C API)
/// @param v3  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTriangleLines koda_wrap_raylib_DrawTriangleLines 4
let DrawTriangleLines = 0;

/// Draws triangle strip.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTriangleStrip koda_wrap_raylib_DrawTriangleStrip 3
let DrawTriangleStrip = 0;

/// Draws triangle strip3 d.
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern DrawTriangleStrip3D koda_wrap_raylib_DrawTriangleStrip3D 3
let DrawTriangleStrip3D = 0;

// ------------------------------------------------------------
//  Files
// ------------------------------------------------------------

/// Calls automation event list.
/// @param list  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportAutomationEventList koda_wrap_raylib_ExportAutomationEventList 2
let ExportAutomationEventList = 0;

/// Calls data as code.
/// @param data  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportDataAsCode koda_wrap_raylib_ExportDataAsCode 3
let ExportDataAsCode = 0;

/// Calls font as code.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportFontAsCode koda_wrap_raylib_ExportFontAsCode 2
let ExportFontAsCode = 0;

/// Calls image.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportImage koda_wrap_raylib_ExportImage 2
let ExportImage = 0;

/// Calls image as code.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportImageAsCode koda_wrap_raylib_ExportImageAsCode 2
let ExportImageAsCode = 0;

/// Calls mesh.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportMesh koda_wrap_raylib_ExportMesh 2
let ExportMesh = 0;

/// Calls mesh as code.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportMeshAsCode koda_wrap_raylib_ExportMeshAsCode 2
let ExportMeshAsCode = 0;

/// Calls wave.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportWave koda_wrap_raylib_ExportWave 2
let ExportWave = 0;

/// Calls wave as code.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ExportWaveAsCode koda_wrap_raylib_ExportWaveAsCode 2
let ExportWaveAsCode = 0;

/// Calls u r l.
/// @param url  string  — null-terminated C string passed as Koda string
// koda:extern OpenURL koda_wrap_raylib_OpenURL 1
let OpenURL = 0;

// ------------------------------------------------------------
//  Frame control
// ------------------------------------------------------------

/// Begins blend mode.
/// @param mode  number  — numeric value
// koda:extern BeginBlendMode koda_wrap_raylib_BeginBlendMode 1
let BeginBlendMode = 0;

/// Begins drawing.
// koda:extern BeginDrawing koda_wrap_raylib_BeginDrawing 0
let BeginDrawing = 0;

/// Begins mode2 d.
/// @param camera  object  — boxed object (struct layout depends on the C API)
// koda:extern BeginMode2D koda_wrap_raylib_BeginMode2D 1
let BeginMode2D = 0;

/// Begins mode3 d.
/// @param camera  object  — boxed object (struct layout depends on the C API)
// koda:extern BeginMode3D koda_wrap_raylib_BeginMode3D 1
let BeginMode3D = 0;

/// Begins scissor mode.
/// @param x  number  — numeric value
/// @param y  number  — numeric value
/// @param width  number  — numeric value
/// @param height  number  — numeric value
// koda:extern BeginScissorMode koda_wrap_raylib_BeginScissorMode 4
let BeginScissorMode = 0;

/// Begins shader mode.
/// @param shader  object  — boxed object (struct layout depends on the C API)
// koda:extern BeginShaderMode koda_wrap_raylib_BeginShaderMode 1
let BeginShaderMode = 0;

/// Begins texture mode.
/// @param target  object  — boxed object (struct layout depends on the C API)
// koda:extern BeginTextureMode koda_wrap_raylib_BeginTextureMode 1
let BeginTextureMode = 0;

/// Begins vr stereo mode.
/// @param config  object  — boxed object (struct layout depends on the C API)
// koda:extern BeginVrStereoMode koda_wrap_raylib_BeginVrStereoMode 1
let BeginVrStereoMode = 0;

/// Ends blend mode.
// koda:extern EndBlendMode koda_wrap_raylib_EndBlendMode 0
let EndBlendMode = 0;

/// Ends drawing.
// koda:extern EndDrawing koda_wrap_raylib_EndDrawing 0
let EndDrawing = 0;

/// Ends mode2 d.
// koda:extern EndMode2D koda_wrap_raylib_EndMode2D 0
let EndMode2D = 0;

/// Ends mode3 d.
// koda:extern EndMode3D koda_wrap_raylib_EndMode3D 0
let EndMode3D = 0;

/// Ends scissor mode.
// koda:extern EndScissorMode koda_wrap_raylib_EndScissorMode 0
let EndScissorMode = 0;

/// Ends shader mode.
// koda:extern EndShaderMode koda_wrap_raylib_EndShaderMode 0
let EndShaderMode = 0;

/// Ends texture mode.
// koda:extern EndTextureMode koda_wrap_raylib_EndTextureMode 0
let EndTextureMode = 0;

/// Ends vr stereo mode.
// koda:extern EndVrStereoMode koda_wrap_raylib_EndVrStereoMode 0
let EndVrStereoMode = 0;

// ------------------------------------------------------------
//  Generation
// ------------------------------------------------------------

/// Calls image cellular.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param tileSize  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenImageCellular koda_wrap_raylib_GenImageCellular 3
let GenImageCellular = 0;

/// Calls image checked.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param checksX  number  — numeric value
/// @param checksY  number  — numeric value
/// @param col1  object  — boxed object (struct layout depends on the C API)
/// @param col2  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenImageChecked koda_wrap_raylib_GenImageChecked 6
let GenImageChecked = 0;

/// Calls image color.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenImageColor koda_wrap_raylib_GenImageColor 3
let GenImageColor = 0;

/// Calls image font atlas.
/// @param glyphs  object  — boxed object (struct layout depends on the C API)
/// @param glyphRecs  object  — boxed object (struct layout depends on the C API)
/// @param glyphCount  number  — numeric value
/// @param fontSize  number  — numeric value
/// @param padding  number  — numeric value
/// @param packMethod  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenImageFontAtlas koda_wrap_raylib_GenImageFontAtlas 6
let GenImageFontAtlas = 0;

/// Calls image gradient linear.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param direction  number  — numeric value
/// @param start  object  — boxed object (struct layout depends on the C API)
/// @param end  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenImageGradientLinear koda_wrap_raylib_GenImageGradientLinear 5
let GenImageGradientLinear = 0;

/// Calls image gradient radial.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param density  number  — numeric value
/// @param inner  object  — boxed object (struct layout depends on the C API)
/// @param outer  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenImageGradientRadial koda_wrap_raylib_GenImageGradientRadial 5
let GenImageGradientRadial = 0;

/// Calls image gradient square.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param density  number  — numeric value
/// @param inner  object  — boxed object (struct layout depends on the C API)
/// @param outer  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenImageGradientSquare koda_wrap_raylib_GenImageGradientSquare 5
let GenImageGradientSquare = 0;

/// Calls image perlin noise.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param offsetX  number  — numeric value
/// @param offsetY  number  — numeric value
/// @param scale  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenImagePerlinNoise koda_wrap_raylib_GenImagePerlinNoise 5
let GenImagePerlinNoise = 0;

/// Calls image text.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param text  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern GenImageText koda_wrap_raylib_GenImageText 3
let GenImageText = 0;

/// Calls image white noise.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param factor  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenImageWhiteNoise koda_wrap_raylib_GenImageWhiteNoise 3
let GenImageWhiteNoise = 0;

/// Calls mesh cone.
/// @param radius  number  — numeric value
/// @param height  number  — numeric value
/// @param slices  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshCone koda_wrap_raylib_GenMeshCone 3
let GenMeshCone = 0;

/// Calls mesh cube.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param length  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshCube koda_wrap_raylib_GenMeshCube 3
let GenMeshCube = 0;

/// Calls mesh cubicmap.
/// @param cubicmap  object  — boxed object (struct layout depends on the C API)
/// @param cubeSize  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenMeshCubicmap koda_wrap_raylib_GenMeshCubicmap 2
let GenMeshCubicmap = 0;

/// Calls mesh cylinder.
/// @param radius  number  — numeric value
/// @param height  number  — numeric value
/// @param slices  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshCylinder koda_wrap_raylib_GenMeshCylinder 3
let GenMeshCylinder = 0;

/// Calls mesh heightmap.
/// @param heightmap  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GenMeshHeightmap koda_wrap_raylib_GenMeshHeightmap 2
let GenMeshHeightmap = 0;

/// Calls mesh hemi sphere.
/// @param radius  number  — numeric value
/// @param rings  number  — numeric value
/// @param slices  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshHemiSphere koda_wrap_raylib_GenMeshHemiSphere 3
let GenMeshHemiSphere = 0;

/// Calls mesh knot.
/// @param radius  number  — numeric value
/// @param size  number  — numeric value
/// @param radSeg  number  — numeric value
/// @param sides  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshKnot koda_wrap_raylib_GenMeshKnot 4
let GenMeshKnot = 0;

/// Calls mesh plane.
/// @param width  number  — numeric value
/// @param length  number  — numeric value
/// @param resX  number  — numeric value
/// @param resZ  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshPlane koda_wrap_raylib_GenMeshPlane 4
let GenMeshPlane = 0;

/// Calls mesh poly.
/// @param sides  number  — numeric value
/// @param radius  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshPoly koda_wrap_raylib_GenMeshPoly 2
let GenMeshPoly = 0;

/// Calls mesh sphere.
/// @param radius  number  — numeric value
/// @param rings  number  — numeric value
/// @param slices  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshSphere koda_wrap_raylib_GenMeshSphere 3
let GenMeshSphere = 0;

/// Calls mesh tangents.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
// koda:extern GenMeshTangents koda_wrap_raylib_GenMeshTangents 1
let GenMeshTangents = 0;

/// Calls mesh torus.
/// @param radius  number  — numeric value
/// @param size  number  — numeric value
/// @param radSeg  number  — numeric value
/// @param sides  number  — numeric value
/// @returns object — result from the native call
// koda:extern GenMeshTorus koda_wrap_raylib_GenMeshTorus 4
let GenMeshTorus = 0;

/// Calls texture mipmaps.
/// @param texture  object  — boxed object (struct layout depends on the C API)
// koda:extern GenTextureMipmaps koda_wrap_raylib_GenTextureMipmaps 1
let GenTextureMipmaps = 0;

// ------------------------------------------------------------
//  Getters
// ------------------------------------------------------------

/// Returns camera matrix.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetCameraMatrix koda_wrap_raylib_GetCameraMatrix 1
let GetCameraMatrix = 0;

/// Returns camera matrix2 d.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetCameraMatrix2D koda_wrap_raylib_GetCameraMatrix2D 1
let GetCameraMatrix2D = 0;

/// Returns char pressed.
/// @returns number — result from the native call
// koda:extern GetCharPressed koda_wrap_raylib_GetCharPressed 0
let GetCharPressed = 0;

/// Returns clipboard image.
/// @returns object — result from the native call
// koda:extern GetClipboardImage koda_wrap_raylib_GetClipboardImage 0
let GetClipboardImage = 0;

/// Returns codepoint.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param codepointSize  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetCodepoint koda_wrap_raylib_GetCodepoint 2
let GetCodepoint = 0;

/// Returns codepoint count.
/// @param text  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern GetCodepointCount koda_wrap_raylib_GetCodepointCount 1
let GetCodepointCount = 0;

/// Returns codepoint next.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param codepointSize  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetCodepointNext koda_wrap_raylib_GetCodepointNext 2
let GetCodepointNext = 0;

/// Returns codepoint previous.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param codepointSize  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetCodepointPrevious koda_wrap_raylib_GetCodepointPrevious 2
let GetCodepointPrevious = 0;

/// Returns collision rec.
/// @param rec1  object  — boxed object (struct layout depends on the C API)
/// @param rec2  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetCollisionRec koda_wrap_raylib_GetCollisionRec 2
let GetCollisionRec = 0;

/// Returns color.
/// @param hexValue  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetColor koda_wrap_raylib_GetColor 1
let GetColor = 0;

/// Returns current monitor.
/// @returns number — result from the native call
// koda:extern GetCurrentMonitor koda_wrap_raylib_GetCurrentMonitor 0
let GetCurrentMonitor = 0;

/// Returns directory file count.
/// @param dirPath  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern GetDirectoryFileCount koda_wrap_raylib_GetDirectoryFileCount 1
let GetDirectoryFileCount = 0;

/// Returns directory file count ex.
/// @param basePath  string  — null-terminated C string passed as Koda string
/// @param filter  string  — null-terminated C string passed as Koda string
/// @param scanSubdirs  bool  — truthy Koda value
/// @returns number — result from the native call
// koda:extern GetDirectoryFileCountEx koda_wrap_raylib_GetDirectoryFileCountEx 3
let GetDirectoryFileCountEx = 0;

/// Returns f p s.
/// @returns number — result from the native call
// koda:extern GetFPS koda_wrap_raylib_GetFPS 0
let GetFPS = 0;

/// Returns file length.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern GetFileLength koda_wrap_raylib_GetFileLength 1
let GetFileLength = 0;

/// Returns file mod time.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern GetFileModTime koda_wrap_raylib_GetFileModTime 1
let GetFileModTime = 0;

/// Returns font default.
/// @returns object — result from the native call
// koda:extern GetFontDefault koda_wrap_raylib_GetFontDefault 0
let GetFontDefault = 0;

/// Returns frame time.
/// @returns number — result from the native call
// koda:extern GetFrameTime koda_wrap_raylib_GetFrameTime 0
let GetFrameTime = 0;

/// Returns gamepad axis count.
/// @param gamepad  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetGamepadAxisCount koda_wrap_raylib_GetGamepadAxisCount 1
let GetGamepadAxisCount = 0;

/// Returns gamepad axis movement.
/// @param gamepad  number  — numeric value
/// @param axis  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetGamepadAxisMovement koda_wrap_raylib_GetGamepadAxisMovement 2
let GetGamepadAxisMovement = 0;

/// Returns gamepad button pressed.
/// @returns number — result from the native call
// koda:extern GetGamepadButtonPressed koda_wrap_raylib_GetGamepadButtonPressed 0
let GetGamepadButtonPressed = 0;

/// Returns gesture detected.
/// @returns number — result from the native call
// koda:extern GetGestureDetected koda_wrap_raylib_GetGestureDetected 0
let GetGestureDetected = 0;

/// Returns gesture drag angle.
/// @returns number — result from the native call
// koda:extern GetGestureDragAngle koda_wrap_raylib_GetGestureDragAngle 0
let GetGestureDragAngle = 0;

/// Returns gesture drag vector.
/// @returns object — result from the native call
// koda:extern GetGestureDragVector koda_wrap_raylib_GetGestureDragVector 0
let GetGestureDragVector = 0;

/// Returns gesture hold duration.
/// @returns number — result from the native call
// koda:extern GetGestureHoldDuration koda_wrap_raylib_GetGestureHoldDuration 0
let GetGestureHoldDuration = 0;

/// Returns gesture pinch angle.
/// @returns number — result from the native call
// koda:extern GetGesturePinchAngle koda_wrap_raylib_GetGesturePinchAngle 0
let GetGesturePinchAngle = 0;

/// Returns gesture pinch vector.
/// @returns object — result from the native call
// koda:extern GetGesturePinchVector koda_wrap_raylib_GetGesturePinchVector 0
let GetGesturePinchVector = 0;

/// Returns glyph atlas rec.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param codepoint  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetGlyphAtlasRec koda_wrap_raylib_GetGlyphAtlasRec 2
let GetGlyphAtlasRec = 0;

/// Returns glyph index.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param codepoint  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetGlyphIndex koda_wrap_raylib_GetGlyphIndex 2
let GetGlyphIndex = 0;

/// Returns glyph info.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param codepoint  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetGlyphInfo koda_wrap_raylib_GetGlyphInfo 2
let GetGlyphInfo = 0;

/// Returns image alpha border.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param threshold  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetImageAlphaBorder koda_wrap_raylib_GetImageAlphaBorder 2
let GetImageAlphaBorder = 0;

/// Returns image color.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param x  number  — numeric value
/// @param y  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetImageColor koda_wrap_raylib_GetImageColor 3
let GetImageColor = 0;

/// Keyboard keys (US keyboard layout) NOTE: Use
/// @returns number — result from the native call
// koda:extern GetKeyPressed koda_wrap_raylib_GetKeyPressed 0
let GetKeyPressed = 0;

/// Returns master volume.
/// @returns number — result from the native call
// koda:extern GetMasterVolume koda_wrap_raylib_GetMasterVolume 0
let GetMasterVolume = 0;

/// Returns mesh bounding box.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetMeshBoundingBox koda_wrap_raylib_GetMeshBoundingBox 1
let GetMeshBoundingBox = 0;

/// Returns model bounding box.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetModelBoundingBox koda_wrap_raylib_GetModelBoundingBox 1
let GetModelBoundingBox = 0;

/// Returns monitor count.
/// @returns number — result from the native call
// koda:extern GetMonitorCount koda_wrap_raylib_GetMonitorCount 0
let GetMonitorCount = 0;

/// Returns monitor height.
/// @param monitor  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetMonitorHeight koda_wrap_raylib_GetMonitorHeight 1
let GetMonitorHeight = 0;

/// Returns monitor physical height.
/// @param monitor  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetMonitorPhysicalHeight koda_wrap_raylib_GetMonitorPhysicalHeight 1
let GetMonitorPhysicalHeight = 0;

/// Returns monitor physical width.
/// @param monitor  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetMonitorPhysicalWidth koda_wrap_raylib_GetMonitorPhysicalWidth 1
let GetMonitorPhysicalWidth = 0;

/// Returns monitor position.
/// @param monitor  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetMonitorPosition koda_wrap_raylib_GetMonitorPosition 1
let GetMonitorPosition = 0;

/// Returns monitor refresh rate.
/// @param monitor  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetMonitorRefreshRate koda_wrap_raylib_GetMonitorRefreshRate 1
let GetMonitorRefreshRate = 0;

/// Returns monitor width.
/// @param monitor  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetMonitorWidth koda_wrap_raylib_GetMonitorWidth 1
let GetMonitorWidth = 0;

/// Returns mouse delta.
/// @returns object — result from the native call
// koda:extern GetMouseDelta koda_wrap_raylib_GetMouseDelta 0
let GetMouseDelta = 0;

/// Returns mouse position.
/// @returns object — result from the native call
// koda:extern GetMousePosition koda_wrap_raylib_GetMousePosition 0
let GetMousePosition = 0;

/// Returns mouse wheel move.
/// @returns number — result from the native call
// koda:extern GetMouseWheelMove koda_wrap_raylib_GetMouseWheelMove 0
let GetMouseWheelMove = 0;

/// Returns mouse wheel move v.
/// @returns object — result from the native call
// koda:extern GetMouseWheelMoveV koda_wrap_raylib_GetMouseWheelMoveV 0
let GetMouseWheelMoveV = 0;

/// Returns mouse x.
/// @returns number — result from the native call
// koda:extern GetMouseX koda_wrap_raylib_GetMouseX 0
let GetMouseX = 0;

/// Returns mouse y.
/// @returns number — result from the native call
// koda:extern GetMouseY koda_wrap_raylib_GetMouseY 0
let GetMouseY = 0;

/// Returns music time length.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @returns number — result from the native call
// koda:extern GetMusicTimeLength koda_wrap_raylib_GetMusicTimeLength 1
let GetMusicTimeLength = 0;

/// Returns music time played.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @returns number — result from the native call
// koda:extern GetMusicTimePlayed koda_wrap_raylib_GetMusicTimePlayed 1
let GetMusicTimePlayed = 0;

/// Returns pixel color.
/// @param srcPtr  object  — boxed object (struct layout depends on the C API)
/// @param format  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetPixelColor koda_wrap_raylib_GetPixelColor 2
let GetPixelColor = 0;

/// Returns pixel data size.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param format  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetPixelDataSize koda_wrap_raylib_GetPixelDataSize 3
let GetPixelDataSize = 0;

/// Returns random value.
/// @param min  number  — numeric value
/// @param max  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetRandomValue koda_wrap_raylib_GetRandomValue 2
let GetRandomValue = 0;

/// Returns ray collision box.
/// @param ray  object  — boxed object (struct layout depends on the C API)
/// @param box  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetRayCollisionBox koda_wrap_raylib_GetRayCollisionBox 2
let GetRayCollisionBox = 0;

/// Returns ray collision mesh.
/// @param ray  object  — boxed object (struct layout depends on the C API)
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param transform  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetRayCollisionMesh koda_wrap_raylib_GetRayCollisionMesh 3
let GetRayCollisionMesh = 0;

/// Returns ray collision quad.
/// @param ray  object  — boxed object (struct layout depends on the C API)
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetRayCollisionQuad koda_wrap_raylib_GetRayCollisionQuad 5
let GetRayCollisionQuad = 0;

/// Returns ray collision sphere.
/// @param ray  object  — boxed object (struct layout depends on the C API)
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetRayCollisionSphere koda_wrap_raylib_GetRayCollisionSphere 3
let GetRayCollisionSphere = 0;

/// Returns ray collision triangle.
/// @param ray  object  — boxed object (struct layout depends on the C API)
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetRayCollisionTriangle koda_wrap_raylib_GetRayCollisionTriangle 4
let GetRayCollisionTriangle = 0;

/// Returns render height.
/// @returns number — result from the native call
// koda:extern GetRenderHeight koda_wrap_raylib_GetRenderHeight 0
let GetRenderHeight = 0;

/// Returns render width.
/// @returns number — result from the native call
// koda:extern GetRenderWidth koda_wrap_raylib_GetRenderWidth 0
let GetRenderWidth = 0;

/// Returns screen height.
/// @returns number — result from the native call
// koda:extern GetScreenHeight koda_wrap_raylib_GetScreenHeight 0
let GetScreenHeight = 0;

/// Returns screen to world2 d.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetScreenToWorld2D koda_wrap_raylib_GetScreenToWorld2D 2
let GetScreenToWorld2D = 0;

/// Returns screen to world ray.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetScreenToWorldRay koda_wrap_raylib_GetScreenToWorldRay 2
let GetScreenToWorldRay = 0;

/// Returns screen to world ray ex.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetScreenToWorldRayEx koda_wrap_raylib_GetScreenToWorldRayEx 4
let GetScreenToWorldRayEx = 0;

/// Returns screen width.
/// @returns number — result from the native call
// koda:extern GetScreenWidth koda_wrap_raylib_GetScreenWidth 0
let GetScreenWidth = 0;

/// Returns shader location.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @param uniformName  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern GetShaderLocation koda_wrap_raylib_GetShaderLocation 2
let GetShaderLocation = 0;

/// Returns shader location attrib.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @param attribName  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern GetShaderLocationAttrib koda_wrap_raylib_GetShaderLocationAttrib 2
let GetShaderLocationAttrib = 0;

/// Returns shapes texture.
/// @returns object — result from the native call
// koda:extern GetShapesTexture koda_wrap_raylib_GetShapesTexture 0
let GetShapesTexture = 0;

/// Returns shapes texture rectangle.
/// @returns object — result from the native call
// koda:extern GetShapesTextureRectangle koda_wrap_raylib_GetShapesTextureRectangle 0
let GetShapesTextureRectangle = 0;

/// Returns spline point basis.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @param t  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetSplinePointBasis koda_wrap_raylib_GetSplinePointBasis 5
let GetSplinePointBasis = 0;

/// Returns spline point bezier cubic.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param c2  object  — boxed object (struct layout depends on the C API)
/// @param c3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @param t  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetSplinePointBezierCubic koda_wrap_raylib_GetSplinePointBezierCubic 5
let GetSplinePointBezierCubic = 0;

/// Returns spline point bezier quad.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param c2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param t  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetSplinePointBezierQuad koda_wrap_raylib_GetSplinePointBezierQuad 4
let GetSplinePointBezierQuad = 0;

/// Returns spline point catmull rom.
/// @param p1  object  — boxed object (struct layout depends on the C API)
/// @param p2  object  — boxed object (struct layout depends on the C API)
/// @param p3  object  — boxed object (struct layout depends on the C API)
/// @param p4  object  — boxed object (struct layout depends on the C API)
/// @param t  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetSplinePointCatmullRom koda_wrap_raylib_GetSplinePointCatmullRom 5
let GetSplinePointCatmullRom = 0;

/// Returns spline point linear.
/// @param startPos  object  — boxed object (struct layout depends on the C API)
/// @param endPos  object  — boxed object (struct layout depends on the C API)
/// @param t  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetSplinePointLinear koda_wrap_raylib_GetSplinePointLinear 3
let GetSplinePointLinear = 0;

/// Returns time.
/// @returns number — result from the native call
// koda:extern GetTime koda_wrap_raylib_GetTime 0
let GetTime = 0;

/// Returns touch point count.
/// @returns number — result from the native call
// koda:extern GetTouchPointCount koda_wrap_raylib_GetTouchPointCount 0
let GetTouchPointCount = 0;

/// Returns touch point id.
/// @param index  number  — numeric value
/// @returns number — result from the native call
// koda:extern GetTouchPointId koda_wrap_raylib_GetTouchPointId 1
let GetTouchPointId = 0;

/// Returns touch position.
/// @param index  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetTouchPosition koda_wrap_raylib_GetTouchPosition 1
let GetTouchPosition = 0;

/// Returns touch x.
/// @returns number — result from the native call
// koda:extern GetTouchX koda_wrap_raylib_GetTouchX 0
let GetTouchX = 0;

/// Returns touch y.
/// @returns number — result from the native call
// koda:extern GetTouchY koda_wrap_raylib_GetTouchY 0
let GetTouchY = 0;

/// Returns window position.
/// @returns object — result from the native call
// koda:extern GetWindowPosition koda_wrap_raylib_GetWindowPosition 0
let GetWindowPosition = 0;

/// Returns window scale d p i.
/// @returns object — result from the native call
// koda:extern GetWindowScaleDPI koda_wrap_raylib_GetWindowScaleDPI 0
let GetWindowScaleDPI = 0;

/// Returns world to screen.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetWorldToScreen koda_wrap_raylib_GetWorldToScreen 2
let GetWorldToScreen = 0;

/// Returns world to screen2 d.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern GetWorldToScreen2D koda_wrap_raylib_GetWorldToScreen2D 2
let GetWorldToScreen2D = 0;

/// Returns world to screen ex.
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @returns object — result from the native call
// koda:extern GetWorldToScreenEx koda_wrap_raylib_GetWorldToScreenEx 4
let GetWorldToScreenEx = 0;

// ------------------------------------------------------------
//  Loading resources
// ------------------------------------------------------------

/// Loads audio stream.
/// @param sampleRate  number  — numeric value
/// @param sampleSize  number  — numeric value
/// @param channels  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadAudioStream koda_wrap_raylib_LoadAudioStream 3
let LoadAudioStream = 0;

/// Loads automation event list.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadAutomationEventList koda_wrap_raylib_LoadAutomationEventList 1
let LoadAutomationEventList = 0;

/// Loads directory files.
/// @param dirPath  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadDirectoryFiles koda_wrap_raylib_LoadDirectoryFiles 1
let LoadDirectoryFiles = 0;

/// Loads directory files ex.
/// @param basePath  string  — null-terminated C string passed as Koda string
/// @param filter  string  — null-terminated C string passed as Koda string
/// @param scanSubdirs  bool  — truthy Koda value
/// @returns object — result from the native call
// koda:extern LoadDirectoryFilesEx koda_wrap_raylib_LoadDirectoryFilesEx 3
let LoadDirectoryFilesEx = 0;

/// Loads dropped files.
/// @returns object — result from the native call
// koda:extern LoadDroppedFiles koda_wrap_raylib_LoadDroppedFiles 0
let LoadDroppedFiles = 0;

/// Loads font.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadFont koda_wrap_raylib_LoadFont 1
let LoadFont = 0;

/// Loads font ex.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param fontSize  number  — numeric value
/// @param codepoints  number  — numeric value
/// @param codepointCount  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadFontEx koda_wrap_raylib_LoadFontEx 4
let LoadFontEx = 0;

/// Loads font from image.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param key  object  — boxed object (struct layout depends on the C API)
/// @param firstChar  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadFontFromImage koda_wrap_raylib_LoadFontFromImage 3
let LoadFontFromImage = 0;

/// Loads font from memory.
/// @param fileType  string  — null-terminated C string passed as Koda string
/// @param fileData  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @param fontSize  number  — numeric value
/// @param codepoints  number  — numeric value
/// @param codepointCount  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadFontFromMemory koda_wrap_raylib_LoadFontFromMemory 6
let LoadFontFromMemory = 0;

/// Loads image.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadImage koda_wrap_raylib_LoadImage 1
let LoadImage = 0;

/// Loads image anim.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param frames  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadImageAnim koda_wrap_raylib_LoadImageAnim 2
let LoadImageAnim = 0;

/// Loads image anim from memory.
/// @param fileType  string  — null-terminated C string passed as Koda string
/// @param fileData  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @param frames  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadImageAnimFromMemory koda_wrap_raylib_LoadImageAnimFromMemory 4
let LoadImageAnimFromMemory = 0;

/// Loads image from memory.
/// @param fileType  string  — null-terminated C string passed as Koda string
/// @param fileData  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadImageFromMemory koda_wrap_raylib_LoadImageFromMemory 3
let LoadImageFromMemory = 0;

/// Loads image from screen.
/// @returns object — result from the native call
// koda:extern LoadImageFromScreen koda_wrap_raylib_LoadImageFromScreen 0
let LoadImageFromScreen = 0;

/// Loads image from texture.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern LoadImageFromTexture koda_wrap_raylib_LoadImageFromTexture 1
let LoadImageFromTexture = 0;

/// Loads image raw.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param format  number  — numeric value
/// @param headerSize  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadImageRaw koda_wrap_raylib_LoadImageRaw 5
let LoadImageRaw = 0;

/// Loads material default.
/// @returns object — result from the native call
// koda:extern LoadMaterialDefault koda_wrap_raylib_LoadMaterialDefault 0
let LoadMaterialDefault = 0;

/// Loads model.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadModel koda_wrap_raylib_LoadModel 1
let LoadModel = 0;

/// Loads model from mesh.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern LoadModelFromMesh koda_wrap_raylib_LoadModelFromMesh 1
let LoadModelFromMesh = 0;

/// Loads music stream.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadMusicStream koda_wrap_raylib_LoadMusicStream 1
let LoadMusicStream = 0;

/// Loads music stream from memory.
/// @param fileType  string  — null-terminated C string passed as Koda string
/// @param data  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadMusicStreamFromMemory koda_wrap_raylib_LoadMusicStreamFromMemory 3
let LoadMusicStreamFromMemory = 0;

/// Loads render texture.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadRenderTexture koda_wrap_raylib_LoadRenderTexture 2
let LoadRenderTexture = 0;

/// Loads shader.
/// @param vsFileName  string  — null-terminated C string passed as Koda string
/// @param fsFileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadShader koda_wrap_raylib_LoadShader 2
let LoadShader = 0;

/// Loads shader from memory.
/// @param vsCode  string  — null-terminated C string passed as Koda string
/// @param fsCode  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadShaderFromMemory koda_wrap_raylib_LoadShaderFromMemory 2
let LoadShaderFromMemory = 0;

/// Loads sound.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadSound koda_wrap_raylib_LoadSound 1
let LoadSound = 0;

/// Loads sound alias.
/// @param source  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern LoadSoundAlias koda_wrap_raylib_LoadSoundAlias 1
let LoadSoundAlias = 0;

/// Loads sound from wave.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern LoadSoundFromWave koda_wrap_raylib_LoadSoundFromWave 1
let LoadSoundFromWave = 0;

/// Loads texture.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadTexture koda_wrap_raylib_LoadTexture 1
let LoadTexture = 0;

/// Loads texture cubemap.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param layout  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadTextureCubemap koda_wrap_raylib_LoadTextureCubemap 2
let LoadTextureCubemap = 0;

/// Loads texture from image.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern LoadTextureFromImage koda_wrap_raylib_LoadTextureFromImage 1
let LoadTextureFromImage = 0;

/// Loads vr stereo config.
/// @param device  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern LoadVrStereoConfig koda_wrap_raylib_LoadVrStereoConfig 1
let LoadVrStereoConfig = 0;

/// Loads wave.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns object — result from the native call
// koda:extern LoadWave koda_wrap_raylib_LoadWave 1
let LoadWave = 0;

/// Loads wave from memory.
/// @param fileType  string  — null-terminated C string passed as Koda string
/// @param fileData  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @returns object — result from the native call
// koda:extern LoadWaveFromMemory koda_wrap_raylib_LoadWaveFromMemory 3
let LoadWaveFromMemory = 0;

/// Unloads audio stream.
/// @param stream  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadAudioStream koda_wrap_raylib_UnloadAudioStream 1
let UnloadAudioStream = 0;

/// Unloads automation event list.
/// @param list  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadAutomationEventList koda_wrap_raylib_UnloadAutomationEventList 1
let UnloadAutomationEventList = 0;

/// Unloads codepoints.
/// @param codepoints  number  — numeric value
// koda:extern UnloadCodepoints koda_wrap_raylib_UnloadCodepoints 1
let UnloadCodepoints = 0;

/// Unloads directory files.
/// @param files  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadDirectoryFiles koda_wrap_raylib_UnloadDirectoryFiles 1
let UnloadDirectoryFiles = 0;

/// Unloads dropped files.
/// @param files  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadDroppedFiles koda_wrap_raylib_UnloadDroppedFiles 1
let UnloadDroppedFiles = 0;

/// Unloads file data.
/// @param data  string  — null-terminated C string passed as Koda string
// koda:extern UnloadFileData koda_wrap_raylib_UnloadFileData 1
let UnloadFileData = 0;

/// Unloads file text.
/// @param text  string  — null-terminated C string passed as Koda string
// koda:extern UnloadFileText koda_wrap_raylib_UnloadFileText 1
let UnloadFileText = 0;

/// Unloads font.
/// @param font  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadFont koda_wrap_raylib_UnloadFont 1
let UnloadFont = 0;

/// Unloads font data.
/// @param glyphs  object  — boxed object (struct layout depends on the C API)
/// @param glyphCount  number  — numeric value
// koda:extern UnloadFontData koda_wrap_raylib_UnloadFontData 2
let UnloadFontData = 0;

/// Unloads image.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadImage koda_wrap_raylib_UnloadImage 1
let UnloadImage = 0;

/// Unloads image colors.
/// @param colors  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadImageColors koda_wrap_raylib_UnloadImageColors 1
let UnloadImageColors = 0;

/// Unloads image palette.
/// @param colors  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadImagePalette koda_wrap_raylib_UnloadImagePalette 1
let UnloadImagePalette = 0;

/// Unloads material.
/// @param material  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadMaterial koda_wrap_raylib_UnloadMaterial 1
let UnloadMaterial = 0;

/// Unloads mesh.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadMesh koda_wrap_raylib_UnloadMesh 1
let UnloadMesh = 0;

/// Unloads model.
/// @param model  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadModel koda_wrap_raylib_UnloadModel 1
let UnloadModel = 0;

/// Unloads model animations.
/// @param animations  object  — boxed object (struct layout depends on the C API)
/// @param animCount  number  — numeric value
// koda:extern UnloadModelAnimations koda_wrap_raylib_UnloadModelAnimations 2
let UnloadModelAnimations = 0;

/// Unloads music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadMusicStream koda_wrap_raylib_UnloadMusicStream 1
let UnloadMusicStream = 0;

/// Unloads random sequence.
/// @param sequence  number  — numeric value
// koda:extern UnloadRandomSequence koda_wrap_raylib_UnloadRandomSequence 1
let UnloadRandomSequence = 0;

/// Unloads render texture.
/// @param target  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadRenderTexture koda_wrap_raylib_UnloadRenderTexture 1
let UnloadRenderTexture = 0;

/// Unloads shader.
/// @param shader  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadShader koda_wrap_raylib_UnloadShader 1
let UnloadShader = 0;

/// Unloads sound.
/// @param sound  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadSound koda_wrap_raylib_UnloadSound 1
let UnloadSound = 0;

/// Unloads sound alias.
/// @param alias  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadSoundAlias koda_wrap_raylib_UnloadSoundAlias 1
let UnloadSoundAlias = 0;

/// Unloads text lines.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param lineCount  number  — numeric value
// koda:extern UnloadTextLines koda_wrap_raylib_UnloadTextLines 2
let UnloadTextLines = 0;

/// Unloads texture.
/// @param texture  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadTexture koda_wrap_raylib_UnloadTexture 1
let UnloadTexture = 0;

/// Unloads u t f8.
/// @param text  string  — null-terminated C string passed as Koda string
// koda:extern UnloadUTF8 koda_wrap_raylib_UnloadUTF8 1
let UnloadUTF8 = 0;

/// Unloads vr stereo config.
/// @param config  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadVrStereoConfig koda_wrap_raylib_UnloadVrStereoConfig 1
let UnloadVrStereoConfig = 0;

/// Unloads wave.
/// @param wave  object  — boxed object (struct layout depends on the C API)
// koda:extern UnloadWave koda_wrap_raylib_UnloadWave 1
let UnloadWave = 0;

/// Unloads wave samples.
/// @param samples  number  — numeric value
// koda:extern UnloadWaveSamples koda_wrap_raylib_UnloadWaveSamples 1
let UnloadWaveSamples = 0;

// ------------------------------------------------------------
//  Memory
// ------------------------------------------------------------

/// Calls free.
/// @param ptr  object  — boxed object (struct layout depends on the C API)
// koda:extern MemFree koda_wrap_raylib_MemFree 1
let MemFree = 0;

// ------------------------------------------------------------
//  Miscellaneous
// ------------------------------------------------------------

/// Calls audio mixed processor.
/// @param processor  object  — boxed object (struct layout depends on the C API)
// koda:extern AttachAudioMixedProcessor koda_wrap_raylib_AttachAudioMixedProcessor 1
let AttachAudioMixedProcessor = 0;

/// Calls audio stream processor.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param processor  object  — boxed object (struct layout depends on the C API)
// koda:extern AttachAudioStreamProcessor koda_wrap_raylib_AttachAudioStreamProcessor 2
let AttachAudioStreamProcessor = 0;

/// Calls directory.
/// @param dirPath  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern ChangeDirectory koda_wrap_raylib_ChangeDirectory 1
let ChangeDirectory = 0;

/// Calls background.
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ClearBackground koda_wrap_raylib_ClearBackground 1
let ClearBackground = 0;

/// Calls window state.
/// @param flags  number  — numeric value
// koda:extern ClearWindowState koda_wrap_raylib_ClearWindowState 1
let ClearWindowState = 0;

/// Calls alpha.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param alpha  number  — numeric value
/// @returns object — result from the native call
// koda:extern ColorAlpha koda_wrap_raylib_ColorAlpha 2
let ColorAlpha = 0;

/// Calls alpha blend.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param src  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ColorAlphaBlend koda_wrap_raylib_ColorAlphaBlend 3
let ColorAlphaBlend = 0;

/// Calls brightness.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param factor  number  — numeric value
/// @returns object — result from the native call
// koda:extern ColorBrightness koda_wrap_raylib_ColorBrightness 2
let ColorBrightness = 0;

/// Calls contrast.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param contrast  number  — numeric value
/// @returns object — result from the native call
// koda:extern ColorContrast koda_wrap_raylib_ColorContrast 2
let ColorContrast = 0;

/// Calls from h s v.
/// @param hue  number  — numeric value
/// @param saturation  number  — numeric value
/// @param value  number  — numeric value
/// @returns object — result from the native call
// koda:extern ColorFromHSV koda_wrap_raylib_ColorFromHSV 3
let ColorFromHSV = 0;

/// Calls from normalized.
/// @param normalized  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ColorFromNormalized koda_wrap_raylib_ColorFromNormalized 1
let ColorFromNormalized = 0;

/// Calls is equal.
/// @param col1  object  — boxed object (struct layout depends on the C API)
/// @param col2  object  — boxed object (struct layout depends on the C API)
/// @returns bool — result from the native call
// koda:extern ColorIsEqual koda_wrap_raylib_ColorIsEqual 2
let ColorIsEqual = 0;

/// Calls lerp.
/// @param color1  object  — boxed object (struct layout depends on the C API)
/// @param color2  object  — boxed object (struct layout depends on the C API)
/// @param factor  number  — numeric value
/// @returns object — result from the native call
// koda:extern ColorLerp koda_wrap_raylib_ColorLerp 3
let ColorLerp = 0;

/// Calls normalize.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ColorNormalize koda_wrap_raylib_ColorNormalize 1
let ColorNormalize = 0;

/// Calls tint.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ColorTint koda_wrap_raylib_ColorTint 2
let ColorTint = 0;

/// Calls to h s v.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ColorToHSV koda_wrap_raylib_ColorToHSV 1
let ColorToHSV = 0;

/// Calls to int.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @returns number — result from the native call
// koda:extern ColorToInt koda_wrap_raylib_ColorToInt 1
let ColorToInt = 0;

/// Calls c r c32.
/// @param data  string  — null-terminated C string passed as Koda string
/// @param dataSize  number  — numeric value
/// @returns number — result from the native call
// koda:extern ComputeCRC32 koda_wrap_raylib_ComputeCRC32 2
let ComputeCRC32 = 0;

/// Calls audio mixed processor.
/// @param processor  object  — boxed object (struct layout depends on the C API)
// koda:extern DetachAudioMixedProcessor koda_wrap_raylib_DetachAudioMixedProcessor 1
let DetachAudioMixedProcessor = 0;

/// Calls audio stream processor.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param processor  object  — boxed object (struct layout depends on the C API)
// koda:extern DetachAudioStreamProcessor koda_wrap_raylib_DetachAudioStreamProcessor 2
let DetachAudioStreamProcessor = 0;

/// Calls exists.
/// @param dirPath  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern DirectoryExists koda_wrap_raylib_DirectoryExists 1
let DirectoryExists = 0;

/// Calls fade.
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param alpha  number  — numeric value
/// @returns object — result from the native call
// koda:extern Fade koda_wrap_raylib_Fade 2
let Fade = 0;

/// Calls copy.
/// @param srcPath  string  — null-terminated C string passed as Koda string
/// @param dstPath  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern FileCopy koda_wrap_raylib_FileCopy 2
let FileCopy = 0;

/// Calls exists.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern FileExists koda_wrap_raylib_FileExists 1
let FileExists = 0;

/// Calls move.
/// @param srcPath  string  — null-terminated C string passed as Koda string
/// @param dstPath  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern FileMove koda_wrap_raylib_FileMove 2
let FileMove = 0;

/// Calls remove.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern FileRemove koda_wrap_raylib_FileRemove 1
let FileRemove = 0;

/// Calls rename.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param fileRename  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern FileRename koda_wrap_raylib_FileRename 2
let FileRename = 0;

/// Calls text find index.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param search  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern FileTextFindIndex koda_wrap_raylib_FileTextFindIndex 2
let FileTextFindIndex = 0;

/// Calls text replace.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param search  string  — null-terminated C string passed as Koda string
/// @param replacement  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern FileTextReplace koda_wrap_raylib_FileTextReplace 3
let FileTextReplace = 0;

/// Calls directory.
/// @param dirPath  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern MakeDirectory koda_wrap_raylib_MakeDirectory 1
let MakeDirectory = 0;

/// Calls window.
// koda:extern MaximizeWindow koda_wrap_raylib_MaximizeWindow 0
let MaximizeWindow = 0;

/// Calls text.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param fontSize  number  — numeric value
/// @returns number — result from the native call
// koda:extern MeasureText koda_wrap_raylib_MeasureText 2
let MeasureText = 0;

/// Calls text codepoints.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param codepoints  number  — numeric value
/// @param length  number  — numeric value
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @returns object — result from the native call
// koda:extern MeasureTextCodepoints koda_wrap_raylib_MeasureTextCodepoints 5
let MeasureTextCodepoints = 0;

/// Calls text ex.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param text  string  — null-terminated C string passed as Koda string
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @returns object — result from the native call
// koda:extern MeasureTextEx koda_wrap_raylib_MeasureTextEx 4
let MeasureTextEx = 0;

/// Calls window.
// koda:extern MinimizeWindow koda_wrap_raylib_MinimizeWindow 0
let MinimizeWindow = 0;

/// Custom frame control functions NOTE: Those functions are intended for advanced users that want full control over the frame processing By default EndDrawing() does this job: draws everything + SwapScreenBuffer() + manage frame timing +
// koda:extern PollInputEvents koda_wrap_raylib_PollInputEvents 0
let PollInputEvents = 0;

/// Calls window.
// koda:extern RestoreWindow koda_wrap_raylib_RestoreWindow 0
let RestoreWindow = 0;

/// Calls audio stream.
/// @param stream  object  — boxed object (struct layout depends on the C API)
// koda:extern ResumeAudioStream koda_wrap_raylib_ResumeAudioStream 1
let ResumeAudioStream = 0;

/// Calls music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
// koda:extern ResumeMusicStream koda_wrap_raylib_ResumeMusicStream 1
let ResumeMusicStream = 0;

/// Calls sound.
/// @param sound  object  — boxed object (struct layout depends on the C API)
// koda:extern ResumeSound koda_wrap_raylib_ResumeSound 1
let ResumeSound = 0;

/// Calls file data.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param data  object  — boxed object (struct layout depends on the C API)
/// @param dataSize  number  — numeric value
/// @returns bool — result from the native call
// koda:extern SaveFileData koda_wrap_raylib_SaveFileData 3
let SaveFileData = 0;

/// Calls file text.
/// @param fileName  string  — null-terminated C string passed as Koda string
/// @param text  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern SaveFileText koda_wrap_raylib_SaveFileText 2
let SaveFileText = 0;

/// Calls music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @param position  number  — numeric value
// koda:extern SeekMusicStream koda_wrap_raylib_SeekMusicStream 2
let SeekMusicStream = 0;

/// Calls automation event recording.
// koda:extern StartAutomationEventRecording koda_wrap_raylib_StartAutomationEventRecording 0
let StartAutomationEventRecording = 0;

/// Custom frame control functions NOTE: Those functions are intended for advanced users that want full control over the frame processing By default EndDrawing() does this job: draws everything +
// koda:extern SwapScreenBuffer koda_wrap_raylib_SwapScreenBuffer 0
let SwapScreenBuffer = 0;

/// Calls screenshot.
/// @param fileName  string  — null-terminated C string passed as Koda string
// koda:extern TakeScreenshot koda_wrap_raylib_TakeScreenshot 1
let TakeScreenshot = 0;

/// Calls borderless windowed.
// koda:extern ToggleBorderlessWindowed koda_wrap_raylib_ToggleBorderlessWindowed 0
let ToggleBorderlessWindowed = 0;

/// Calls fullscreen.
// koda:extern ToggleFullscreen koda_wrap_raylib_ToggleFullscreen 0
let ToggleFullscreen = 0;

/// Calls mesh.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param dynamic  bool  — truthy Koda value
// koda:extern UploadMesh koda_wrap_raylib_UploadMesh 2
let UploadMesh = 0;

/// Calls time.
/// @param seconds  number  — numeric value
// koda:extern WaitTime koda_wrap_raylib_WaitTime 1
let WaitTime = 0;

/// Calls copy.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern WaveCopy koda_wrap_raylib_WaveCopy 1
let WaveCopy = 0;

/// Calls crop.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @param initFrame  number  — numeric value
/// @param finalFrame  number  — numeric value
// koda:extern WaveCrop koda_wrap_raylib_WaveCrop 3
let WaveCrop = 0;

/// Calls format.
/// @param wave  object  — boxed object (struct layout depends on the C API)
/// @param sampleRate  number  — numeric value
/// @param sampleSize  number  — numeric value
/// @param channels  number  — numeric value
// koda:extern WaveFormat koda_wrap_raylib_WaveFormat 4
let WaveFormat = 0;

// ------------------------------------------------------------
//  Setters
// ------------------------------------------------------------

/// Sets audio stream buffer size default.
/// @param size  number  — numeric value
// koda:extern SetAudioStreamBufferSizeDefault koda_wrap_raylib_SetAudioStreamBufferSizeDefault 1
let SetAudioStreamBufferSizeDefault = 0;

/// Sets audio stream callback.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param callback  object  — boxed object (struct layout depends on the C API)
// koda:extern SetAudioStreamCallback koda_wrap_raylib_SetAudioStreamCallback 2
let SetAudioStreamCallback = 0;

/// Sets audio stream pan.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param pan  number  — numeric value
// koda:extern SetAudioStreamPan koda_wrap_raylib_SetAudioStreamPan 2
let SetAudioStreamPan = 0;

/// Sets audio stream pitch.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param pitch  number  — numeric value
// koda:extern SetAudioStreamPitch koda_wrap_raylib_SetAudioStreamPitch 2
let SetAudioStreamPitch = 0;

/// Sets audio stream volume.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param volume  number  — numeric value
// koda:extern SetAudioStreamVolume koda_wrap_raylib_SetAudioStreamVolume 2
let SetAudioStreamVolume = 0;

/// Sets automation event base frame.
/// @param frame  number  — numeric value
// koda:extern SetAutomationEventBaseFrame koda_wrap_raylib_SetAutomationEventBaseFrame 1
let SetAutomationEventBaseFrame = 0;

/// Sets automation event list.
/// @param list  object  — boxed object (struct layout depends on the C API)
// koda:extern SetAutomationEventList koda_wrap_raylib_SetAutomationEventList 1
let SetAutomationEventList = 0;

/// Sets clipboard text.
/// @param text  string  — null-terminated C string passed as Koda string
// koda:extern SetClipboardText koda_wrap_raylib_SetClipboardText 1
let SetClipboardText = 0;

/// Sets config flags.
/// @param flags  number  — numeric value
// koda:extern SetConfigFlags koda_wrap_raylib_SetConfigFlags 1
let SetConfigFlags = 0;

/// Sets exit key.
/// @param key  number  — numeric value
// koda:extern SetExitKey koda_wrap_raylib_SetExitKey 1
let SetExitKey = 0;

/// Sets gamepad mappings.
/// @param mappings  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern SetGamepadMappings koda_wrap_raylib_SetGamepadMappings 1
let SetGamepadMappings = 0;

/// Sets gamepad vibration.
/// @param gamepad  number  — numeric value
/// @param leftMotor  number  — numeric value
/// @param rightMotor  number  — numeric value
/// @param duration  number  — numeric value
// koda:extern SetGamepadVibration koda_wrap_raylib_SetGamepadVibration 4
let SetGamepadVibration = 0;

/// Sets gestures enabled.
/// @param flags  number  — numeric value
// koda:extern SetGesturesEnabled koda_wrap_raylib_SetGesturesEnabled 1
let SetGesturesEnabled = 0;

/// Sets load file data callback.
/// @param callback  object  — boxed object (struct layout depends on the C API)
// koda:extern SetLoadFileDataCallback koda_wrap_raylib_SetLoadFileDataCallback 1
let SetLoadFileDataCallback = 0;

/// Sets load file text callback.
/// @param callback  object  — boxed object (struct layout depends on the C API)
// koda:extern SetLoadFileTextCallback koda_wrap_raylib_SetLoadFileTextCallback 1
let SetLoadFileTextCallback = 0;

/// Sets master volume.
/// @param volume  number  — numeric value
// koda:extern SetMasterVolume koda_wrap_raylib_SetMasterVolume 1
let SetMasterVolume = 0;

/// Sets material texture.
/// @param material  object  — boxed object (struct layout depends on the C API)
/// @param mapType  number  — numeric value
/// @param texture  object  — boxed object (struct layout depends on the C API)
// koda:extern SetMaterialTexture koda_wrap_raylib_SetMaterialTexture 3
let SetMaterialTexture = 0;

/// Sets model mesh material.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param meshId  number  — numeric value
/// @param materialId  number  — numeric value
// koda:extern SetModelMeshMaterial koda_wrap_raylib_SetModelMeshMaterial 3
let SetModelMeshMaterial = 0;

/// Sets mouse cursor.
/// @param cursor  number  — numeric value
// koda:extern SetMouseCursor koda_wrap_raylib_SetMouseCursor 1
let SetMouseCursor = 0;

/// Sets mouse offset.
/// @param offsetX  number  — numeric value
/// @param offsetY  number  — numeric value
// koda:extern SetMouseOffset koda_wrap_raylib_SetMouseOffset 2
let SetMouseOffset = 0;

/// Sets mouse position.
/// @param x  number  — numeric value
/// @param y  number  — numeric value
// koda:extern SetMousePosition koda_wrap_raylib_SetMousePosition 2
let SetMousePosition = 0;

/// Sets mouse scale.
/// @param scaleX  number  — numeric value
/// @param scaleY  number  — numeric value
// koda:extern SetMouseScale koda_wrap_raylib_SetMouseScale 2
let SetMouseScale = 0;

/// Sets music pan.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @param pan  number  — numeric value
// koda:extern SetMusicPan koda_wrap_raylib_SetMusicPan 2
let SetMusicPan = 0;

/// Sets music pitch.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @param pitch  number  — numeric value
// koda:extern SetMusicPitch koda_wrap_raylib_SetMusicPitch 2
let SetMusicPitch = 0;

/// Sets music volume.
/// @param music  object  — boxed object (struct layout depends on the C API)
/// @param volume  number  — numeric value
// koda:extern SetMusicVolume koda_wrap_raylib_SetMusicVolume 2
let SetMusicVolume = 0;

/// Sets pixel color.
/// @param dstPtr  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param format  number  — numeric value
// koda:extern SetPixelColor koda_wrap_raylib_SetPixelColor 3
let SetPixelColor = 0;

/// Sets random seed.
/// @param seed  number  — numeric value
// koda:extern SetRandomSeed koda_wrap_raylib_SetRandomSeed 1
let SetRandomSeed = 0;

/// Sets save file data callback.
/// @param callback  object  — boxed object (struct layout depends on the C API)
// koda:extern SetSaveFileDataCallback koda_wrap_raylib_SetSaveFileDataCallback 1
let SetSaveFileDataCallback = 0;

/// Sets save file text callback.
/// @param callback  object  — boxed object (struct layout depends on the C API)
// koda:extern SetSaveFileTextCallback koda_wrap_raylib_SetSaveFileTextCallback 1
let SetSaveFileTextCallback = 0;

/// Sets shader value.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @param locIndex  number  — numeric value
/// @param value  object  — boxed object (struct layout depends on the C API)
/// @param uniformType  number  — numeric value
// koda:extern SetShaderValue koda_wrap_raylib_SetShaderValue 4
let SetShaderValue = 0;

/// Sets shader value matrix.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @param locIndex  number  — numeric value
/// @param mat  object  — boxed object (struct layout depends on the C API)
// koda:extern SetShaderValueMatrix koda_wrap_raylib_SetShaderValueMatrix 3
let SetShaderValueMatrix = 0;

/// Sets shader value texture.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @param locIndex  number  — numeric value
/// @param texture  object  — boxed object (struct layout depends on the C API)
// koda:extern SetShaderValueTexture koda_wrap_raylib_SetShaderValueTexture 3
let SetShaderValueTexture = 0;

/// Sets shader value v.
/// @param shader  object  — boxed object (struct layout depends on the C API)
/// @param locIndex  number  — numeric value
/// @param value  object  — boxed object (struct layout depends on the C API)
/// @param uniformType  number  — numeric value
/// @param count  number  — numeric value
// koda:extern SetShaderValueV koda_wrap_raylib_SetShaderValueV 5
let SetShaderValueV = 0;

/// Sets shapes texture.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param source  object  — boxed object (struct layout depends on the C API)
// koda:extern SetShapesTexture koda_wrap_raylib_SetShapesTexture 2
let SetShapesTexture = 0;

/// Sets sound pan.
/// @param sound  object  — boxed object (struct layout depends on the C API)
/// @param pan  number  — numeric value
// koda:extern SetSoundPan koda_wrap_raylib_SetSoundPan 2
let SetSoundPan = 0;

/// Sets sound pitch.
/// @param sound  object  — boxed object (struct layout depends on the C API)
/// @param pitch  number  — numeric value
// koda:extern SetSoundPitch koda_wrap_raylib_SetSoundPitch 2
let SetSoundPitch = 0;

/// Sets sound volume.
/// @param sound  object  — boxed object (struct layout depends on the C API)
/// @param volume  number  — numeric value
// koda:extern SetSoundVolume koda_wrap_raylib_SetSoundVolume 2
let SetSoundVolume = 0;

/// Sets target f p s.
/// @param fps  number  — numeric value
// koda:extern SetTargetFPS koda_wrap_raylib_SetTargetFPS 1
let SetTargetFPS = 0;

/// Sets text line spacing.
/// @param spacing  number  — numeric value
// koda:extern SetTextLineSpacing koda_wrap_raylib_SetTextLineSpacing 1
let SetTextLineSpacing = 0;

/// Sets texture filter.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param filter  number  — numeric value
// koda:extern SetTextureFilter koda_wrap_raylib_SetTextureFilter 2
let SetTextureFilter = 0;

/// Sets texture wrap.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param wrap  number  — numeric value
// koda:extern SetTextureWrap koda_wrap_raylib_SetTextureWrap 2
let SetTextureWrap = 0;

/// Sets trace log callback.
/// @param callback  object  — boxed object (struct layout depends on the C API)
// koda:extern SetTraceLogCallback koda_wrap_raylib_SetTraceLogCallback 1
let SetTraceLogCallback = 0;

/// Sets trace log level.
/// @param logLevel  number  — numeric value
// koda:extern SetTraceLogLevel koda_wrap_raylib_SetTraceLogLevel 1
let SetTraceLogLevel = 0;

/// Sets window focused.
// koda:extern SetWindowFocused koda_wrap_raylib_SetWindowFocused 0
let SetWindowFocused = 0;

/// Sets window icon.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern SetWindowIcon koda_wrap_raylib_SetWindowIcon 1
let SetWindowIcon = 0;

/// Sets window icons.
/// @param images  object  — boxed object (struct layout depends on the C API)
/// @param count  number  — numeric value
// koda:extern SetWindowIcons koda_wrap_raylib_SetWindowIcons 2
let SetWindowIcons = 0;

/// Sets window max size.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
// koda:extern SetWindowMaxSize koda_wrap_raylib_SetWindowMaxSize 2
let SetWindowMaxSize = 0;

/// Sets window min size.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
// koda:extern SetWindowMinSize koda_wrap_raylib_SetWindowMinSize 2
let SetWindowMinSize = 0;

/// Sets window monitor.
/// @param monitor  number  — numeric value
// koda:extern SetWindowMonitor koda_wrap_raylib_SetWindowMonitor 1
let SetWindowMonitor = 0;

/// Sets window opacity.
/// @param opacity  number  — numeric value
// koda:extern SetWindowOpacity koda_wrap_raylib_SetWindowOpacity 1
let SetWindowOpacity = 0;

/// Sets window position.
/// @param x  number  — numeric value
/// @param y  number  — numeric value
// koda:extern SetWindowPosition koda_wrap_raylib_SetWindowPosition 2
let SetWindowPosition = 0;

/// Sets window size.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
// koda:extern SetWindowSize koda_wrap_raylib_SetWindowSize 2
let SetWindowSize = 0;

/// Sets window state.
/// @param flags  number  — numeric value
// koda:extern SetWindowState koda_wrap_raylib_SetWindowState 1
let SetWindowState = 0;

/// Sets window title.
/// @param title  string  — null-terminated C string passed as Koda string
// koda:extern SetWindowTitle koda_wrap_raylib_SetWindowTitle 1
let SetWindowTitle = 0;

// ------------------------------------------------------------
//  Text
// ------------------------------------------------------------

/// Calls append.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param append  string  — null-terminated C string passed as Koda string
/// @param position  number  — numeric value
// koda:extern TextAppend koda_wrap_raylib_TextAppend 3
let TextAppend = 0;

/// Calls copy.
/// @param dst  string  — null-terminated C string passed as Koda string
/// @param src  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern TextCopy koda_wrap_raylib_TextCopy 2
let TextCopy = 0;

/// Calls find index.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param search  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern TextFindIndex koda_wrap_raylib_TextFindIndex 2
let TextFindIndex = 0;

/// Calls is equal.
/// @param text1  string  — null-terminated C string passed as Koda string
/// @param text2  string  — null-terminated C string passed as Koda string
/// @returns bool — result from the native call
// koda:extern TextIsEqual koda_wrap_raylib_TextIsEqual 2
let TextIsEqual = 0;

/// Calls length.
/// @param text  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern TextLength koda_wrap_raylib_TextLength 1
let TextLength = 0;

/// Calls to float.
/// @param text  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern TextToFloat koda_wrap_raylib_TextToFloat 1
let TextToFloat = 0;

/// Calls to integer.
/// @param text  string  — null-terminated C string passed as Koda string
/// @returns number — result from the native call
// koda:extern TextToInteger koda_wrap_raylib_TextToInteger 1
let TextToInteger = 0;

// ------------------------------------------------------------
//  Textures
// ------------------------------------------------------------

/// Calls alpha clear.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param threshold  number  — numeric value
// koda:extern ImageAlphaClear koda_wrap_raylib_ImageAlphaClear 3
let ImageAlphaClear = 0;

/// Calls alpha crop.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param threshold  number  — numeric value
// koda:extern ImageAlphaCrop koda_wrap_raylib_ImageAlphaCrop 2
let ImageAlphaCrop = 0;

/// Calls alpha mask.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param alphaMask  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageAlphaMask koda_wrap_raylib_ImageAlphaMask 2
let ImageAlphaMask = 0;

/// Calls alpha premultiply.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageAlphaPremultiply koda_wrap_raylib_ImageAlphaPremultiply 1
let ImageAlphaPremultiply = 0;

/// Calls blur gaussian.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param blurSize  number  — numeric value
// koda:extern ImageBlurGaussian koda_wrap_raylib_ImageBlurGaussian 2
let ImageBlurGaussian = 0;

/// Calls clear background.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageClearBackground koda_wrap_raylib_ImageClearBackground 2
let ImageClearBackground = 0;

/// Calls color brightness.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param brightness  number  — numeric value
// koda:extern ImageColorBrightness koda_wrap_raylib_ImageColorBrightness 2
let ImageColorBrightness = 0;

/// Calls color contrast.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param contrast  number  — numeric value
// koda:extern ImageColorContrast koda_wrap_raylib_ImageColorContrast 2
let ImageColorContrast = 0;

/// Calls color grayscale.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageColorGrayscale koda_wrap_raylib_ImageColorGrayscale 1
let ImageColorGrayscale = 0;

/// Calls color invert.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageColorInvert koda_wrap_raylib_ImageColorInvert 1
let ImageColorInvert = 0;

/// Calls color replace.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @param replace  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageColorReplace koda_wrap_raylib_ImageColorReplace 3
let ImageColorReplace = 0;

/// Calls color tint.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageColorTint koda_wrap_raylib_ImageColorTint 2
let ImageColorTint = 0;

/// Calls copy.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ImageCopy koda_wrap_raylib_ImageCopy 1
let ImageCopy = 0;

/// Calls crop.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param crop  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageCrop koda_wrap_raylib_ImageCrop 2
let ImageCrop = 0;

/// Calls dither.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param rBpp  number  — numeric value
/// @param gBpp  number  — numeric value
/// @param bBpp  number  — numeric value
/// @param aBpp  number  — numeric value
// koda:extern ImageDither koda_wrap_raylib_ImageDither 5
let ImageDither = 0;

/// Calls draw.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param src  object  — boxed object (struct layout depends on the C API)
/// @param srcRec  object  — boxed object (struct layout depends on the C API)
/// @param dstRec  object  — boxed object (struct layout depends on the C API)
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDraw koda_wrap_raylib_ImageDraw 5
let ImageDraw = 0;

/// Calls draw circle.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param centerX  number  — numeric value
/// @param centerY  number  — numeric value
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawCircle koda_wrap_raylib_ImageDrawCircle 5
let ImageDrawCircle = 0;

/// Calls draw circle lines.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param centerX  number  — numeric value
/// @param centerY  number  — numeric value
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawCircleLines koda_wrap_raylib_ImageDrawCircleLines 5
let ImageDrawCircleLines = 0;

/// Calls draw circle lines v.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawCircleLinesV koda_wrap_raylib_ImageDrawCircleLinesV 4
let ImageDrawCircleLinesV = 0;

/// Calls draw circle v.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param center  object  — boxed object (struct layout depends on the C API)
/// @param radius  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawCircleV koda_wrap_raylib_ImageDrawCircleV 4
let ImageDrawCircleV = 0;

/// Calls draw line.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param startPosX  number  — numeric value
/// @param startPosY  number  — numeric value
/// @param endPosX  number  — numeric value
/// @param endPosY  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawLine koda_wrap_raylib_ImageDrawLine 6
let ImageDrawLine = 0;

/// Calls draw line ex.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param start  object  — boxed object (struct layout depends on the C API)
/// @param end  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawLineEx koda_wrap_raylib_ImageDrawLineEx 5
let ImageDrawLineEx = 0;

/// Calls draw line v.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param start  object  — boxed object (struct layout depends on the C API)
/// @param end  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawLineV koda_wrap_raylib_ImageDrawLineV 4
let ImageDrawLineV = 0;

/// Calls draw pixel.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawPixel koda_wrap_raylib_ImageDrawPixel 4
let ImageDrawPixel = 0;

/// Calls draw pixel v.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawPixelV koda_wrap_raylib_ImageDrawPixelV 3
let ImageDrawPixelV = 0;

/// Calls draw rectangle.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawRectangle koda_wrap_raylib_ImageDrawRectangle 6
let ImageDrawRectangle = 0;

/// Calls draw rectangle lines.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param thick  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawRectangleLines koda_wrap_raylib_ImageDrawRectangleLines 4
let ImageDrawRectangleLines = 0;

/// Calls draw rectangle rec.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawRectangleRec koda_wrap_raylib_ImageDrawRectangleRec 3
let ImageDrawRectangleRec = 0;

/// Calls draw rectangle v.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param size  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawRectangleV koda_wrap_raylib_ImageDrawRectangleV 4
let ImageDrawRectangleV = 0;

/// Calls draw text.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param text  string  — null-terminated C string passed as Koda string
/// @param posX  number  — numeric value
/// @param posY  number  — numeric value
/// @param fontSize  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawText koda_wrap_raylib_ImageDrawText 6
let ImageDrawText = 0;

/// Calls draw text ex.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param text  string  — null-terminated C string passed as Koda string
/// @param position  object  — boxed object (struct layout depends on the C API)
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawTextEx koda_wrap_raylib_ImageDrawTextEx 7
let ImageDrawTextEx = 0;

/// Calls draw triangle.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param v1  object  — boxed object (struct layout depends on the C API)
/// @param v2  object  — boxed object (struct layout depends on the C API)
/// @param v3  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawTriangle koda_wrap_raylib_ImageDrawTriangle 5
let ImageDrawTriangle = 0;

/// Calls draw triangle ex.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param v1  object  — boxed object (struct layout depends on the C API)
/// @param v2  object  — boxed object (struct layout depends on the C API)
/// @param v3  object  — boxed object (struct layout depends on the C API)
/// @param c1  object  — boxed object (struct layout depends on the C API)
/// @param c2  object  — boxed object (struct layout depends on the C API)
/// @param c3  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawTriangleEx koda_wrap_raylib_ImageDrawTriangleEx 7
let ImageDrawTriangleEx = 0;

/// Calls draw triangle fan.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawTriangleFan koda_wrap_raylib_ImageDrawTriangleFan 4
let ImageDrawTriangleFan = 0;

/// Calls draw triangle lines.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param v1  object  — boxed object (struct layout depends on the C API)
/// @param v2  object  — boxed object (struct layout depends on the C API)
/// @param v3  object  — boxed object (struct layout depends on the C API)
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawTriangleLines koda_wrap_raylib_ImageDrawTriangleLines 5
let ImageDrawTriangleLines = 0;

/// Calls draw triangle strip.
/// @param dst  object  — boxed object (struct layout depends on the C API)
/// @param points  object  — boxed object (struct layout depends on the C API)
/// @param pointCount  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageDrawTriangleStrip koda_wrap_raylib_ImageDrawTriangleStrip 4
let ImageDrawTriangleStrip = 0;

/// Calls flip horizontal.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageFlipHorizontal koda_wrap_raylib_ImageFlipHorizontal 1
let ImageFlipHorizontal = 0;

/// Calls flip vertical.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageFlipVertical koda_wrap_raylib_ImageFlipVertical 1
let ImageFlipVertical = 0;

/// Calls format.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param newFormat  number  — numeric value
// koda:extern ImageFormat koda_wrap_raylib_ImageFormat 2
let ImageFormat = 0;

/// Calls from channel.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param selectedChannel  number  — numeric value
/// @returns object — result from the native call
// koda:extern ImageFromChannel koda_wrap_raylib_ImageFromChannel 2
let ImageFromChannel = 0;

/// Calls from image.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ImageFromImage koda_wrap_raylib_ImageFromImage 2
let ImageFromImage = 0;

/// Calls kernel convolution.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param kernel  number  — numeric value
/// @param kernelSize  number  — numeric value
// koda:extern ImageKernelConvolution koda_wrap_raylib_ImageKernelConvolution 3
let ImageKernelConvolution = 0;

/// Calls mipmaps.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageMipmaps koda_wrap_raylib_ImageMipmaps 1
let ImageMipmaps = 0;

/// Calls resize.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param newWidth  number  — numeric value
/// @param newHeight  number  — numeric value
// koda:extern ImageResize koda_wrap_raylib_ImageResize 3
let ImageResize = 0;

/// Calls resize canvas.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param newWidth  number  — numeric value
/// @param newHeight  number  — numeric value
/// @param offsetX  number  — numeric value
/// @param offsetY  number  — numeric value
/// @param fill  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageResizeCanvas koda_wrap_raylib_ImageResizeCanvas 6
let ImageResizeCanvas = 0;

/// Calls resize n n.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param newWidth  number  — numeric value
/// @param newHeight  number  — numeric value
// koda:extern ImageResizeNN koda_wrap_raylib_ImageResizeNN 3
let ImageResizeNN = 0;

/// Calls rotate.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param degrees  number  — numeric value
// koda:extern ImageRotate koda_wrap_raylib_ImageRotate 2
let ImageRotate = 0;

/// Calls rotate c c w.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageRotateCCW koda_wrap_raylib_ImageRotateCCW 1
let ImageRotateCCW = 0;

/// Calls rotate c w.
/// @param image  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageRotateCW koda_wrap_raylib_ImageRotateCW 1
let ImageRotateCW = 0;

/// Calls text.
/// @param text  string  — null-terminated C string passed as Koda string
/// @param fontSize  number  — numeric value
/// @param color  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ImageText koda_wrap_raylib_ImageText 3
let ImageText = 0;

/// Calls text ex.
/// @param font  object  — boxed object (struct layout depends on the C API)
/// @param text  string  — null-terminated C string passed as Koda string
/// @param fontSize  number  — numeric value
/// @param spacing  number  — numeric value
/// @param tint  object  — boxed object (struct layout depends on the C API)
/// @returns object — result from the native call
// koda:extern ImageTextEx koda_wrap_raylib_ImageTextEx 5
let ImageTextEx = 0;

/// Calls to p o t.
/// @param image  object  — boxed object (struct layout depends on the C API)
/// @param fill  object  — boxed object (struct layout depends on the C API)
// koda:extern ImageToPOT koda_wrap_raylib_ImageToPOT 2
let ImageToPOT = 0;

// ------------------------------------------------------------
//  UI
// ------------------------------------------------------------

/// Hides cursor.
// koda:extern HideCursor koda_wrap_raylib_HideCursor 0
let HideCursor = 0;

/// Shows cursor.
// koda:extern ShowCursor koda_wrap_raylib_ShowCursor 0
let ShowCursor = 0;

// ------------------------------------------------------------
//  Update
// ------------------------------------------------------------

/// Updates audio stream.
/// @param stream  object  — boxed object (struct layout depends on the C API)
/// @param data  object  — boxed object (struct layout depends on the C API)
/// @param frameCount  number  — numeric value
// koda:extern UpdateAudioStream koda_wrap_raylib_UpdateAudioStream 3
let UpdateAudioStream = 0;

/// Updates camera.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param mode  number  — numeric value
// koda:extern UpdateCamera koda_wrap_raylib_UpdateCamera 2
let UpdateCamera = 0;

/// Updates camera pro.
/// @param camera  object  — boxed object (struct layout depends on the C API)
/// @param movement  object  — boxed object (struct layout depends on the C API)
/// @param rotation  object  — boxed object (struct layout depends on the C API)
/// @param zoom  number  — numeric value
// koda:extern UpdateCameraPro koda_wrap_raylib_UpdateCameraPro 4
let UpdateCameraPro = 0;

/// Updates mesh buffer.
/// @param mesh  object  — boxed object (struct layout depends on the C API)
/// @param index  number  — numeric value
/// @param data  object  — boxed object (struct layout depends on the C API)
/// @param dataSize  number  — numeric value
/// @param offset  number  — numeric value
// koda:extern UpdateMeshBuffer koda_wrap_raylib_UpdateMeshBuffer 5
let UpdateMeshBuffer = 0;

/// Updates model animation.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param anim  object  — boxed object (struct layout depends on the C API)
/// @param frame  number  — numeric value
// koda:extern UpdateModelAnimation koda_wrap_raylib_UpdateModelAnimation 3
let UpdateModelAnimation = 0;

/// Updates model animation ex.
/// @param model  object  — boxed object (struct layout depends on the C API)
/// @param animA  object  — boxed object (struct layout depends on the C API)
/// @param frameA  number  — numeric value
/// @param animB  object  — boxed object (struct layout depends on the C API)
/// @param frameB  number  — numeric value
/// @param blend  number  — numeric value
// koda:extern UpdateModelAnimationEx koda_wrap_raylib_UpdateModelAnimationEx 6
let UpdateModelAnimationEx = 0;

/// Updates music stream.
/// @param music  object  — boxed object (struct layout depends on the C API)
// koda:extern UpdateMusicStream koda_wrap_raylib_UpdateMusicStream 1
let UpdateMusicStream = 0;

/// Updates sound.
/// @param sound  object  — boxed object (struct layout depends on the C API)
/// @param data  object  — boxed object (struct layout depends on the C API)
/// @param sampleCount  number  — numeric value
// koda:extern UpdateSound koda_wrap_raylib_UpdateSound 3
let UpdateSound = 0;

/// Updates texture.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param pixels  object  — boxed object (struct layout depends on the C API)
// koda:extern UpdateTexture koda_wrap_raylib_UpdateTexture 2
let UpdateTexture = 0;

/// Updates texture rec.
/// @param texture  object  — boxed object (struct layout depends on the C API)
/// @param rec  object  — boxed object (struct layout depends on the C API)
/// @param pixels  object  — boxed object (struct layout depends on the C API)
// koda:extern UpdateTextureRec koda_wrap_raylib_UpdateTextureRec 3
let UpdateTextureRec = 0;

// ------------------------------------------------------------
//  Window
// ------------------------------------------------------------

/// Initialises window.
/// @param width  number  — numeric value
/// @param height  number  — numeric value
/// @param title  string  — null-terminated C string passed as Koda string
// koda:extern InitWindow koda_wrap_raylib_InitWindow 3
let InitWindow = 0;

/// Calls should close.
/// @returns bool — result from the native call
// koda:extern WindowShouldClose koda_wrap_raylib_WindowShouldClose 0
let WindowShouldClose = 0;

