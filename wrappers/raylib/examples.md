# raylib — examples

Generated by **kodawrap**. Copy into your project after linking `wrapper.c`.

```koda
#include "@raylib"

func main() {
    InitWindow(800, 450, "Hello");
}
```

## Audio

```koda
InitAudioDevice();
```

```koda
PauseAudioStream(stream);
```

```koda
PauseMusicStream(music);
```

```koda
PauseSound(sound);
```

```koda
PlayAudioStream(stream);
```

_…and 7 more in [api_reference.md](api_reference.md)._

## Checks

```koda
CheckCollisionBoxSphere(box, center, radius);
```

```koda
CheckCollisionBoxes(box1, box2);
```

```koda
CheckCollisionCircleLine(center, radius, p1, p2);
```

```koda
CheckCollisionCircleRec(center, radius, rec);
```

```koda
CheckCollisionCircles(center1, radius1, center2, radius2);
```

_…and 54 more in [api_reference.md](api_reference.md)._

## Cleanup

```koda
CloseAudioDevice();
```

```koda
CloseWindow();
```

## Configuration

```koda
DisableCursor();
```

```koda
DisableEventWaiting();
```

```koda
EnableCursor();
```

```koda
EnableEventWaiting();
```

## Debug

```koda
TraceLog(logLevel, text);
```

## Drawing

```koda
DrawBillboard(camera, texture, position, scale, tint);
```

```koda
DrawBillboardPro(camera, texture, source, position, up, size, origin, rotation, tint);
```

```koda
DrawBillboardRec(camera, texture, source, position, size, tint);
```

```koda
DrawBoundingBox(box, color);
```

```koda
DrawCapsule(startPos, endPos, radius, slices, rings, color);
```

_…and 88 more in [api_reference.md](api_reference.md)._

## Files

```koda
ExportAutomationEventList(list, fileName);
```

```koda
ExportDataAsCode(data, dataSize, fileName);
```

```koda
ExportFontAsCode(font, fileName);
```

```koda
ExportImage(image, fileName);
```

```koda
ExportImageAsCode(image, fileName);
```

_…and 5 more in [api_reference.md](api_reference.md)._

## Frame control

```koda
BeginBlendMode(mode);
```

```koda
BeginDrawing();
```

```koda
BeginMode2D(camera);
```

```koda
BeginMode3D(camera);
```

```koda
BeginScissorMode(x, y, width, height);
```

_…and 11 more in [api_reference.md](api_reference.md)._

## Generation

```koda
GenImageCellular(width, height, tileSize);
```

```koda
GenImageChecked(width, height, checksX, checksY, col1, col2);
```

```koda
GenImageColor(width, height, color);
```

```koda
GenImageFontAtlas(glyphs, glyphRecs, glyphCount, fontSize, padding, packMethod);
```

```koda
GenImageGradientLinear(width, height, direction, start, end);
```

_…and 18 more in [api_reference.md](api_reference.md)._

## Getters

```koda
GetCameraMatrix(camera);
```

```koda
GetCameraMatrix2D(camera);
```

```koda
GetCharPressed();
```

```koda
GetClipboardImage();
```

```koda
GetCodepoint(text, codepointSize);
```

_…and 81 more in [api_reference.md](api_reference.md)._

## Loading resources

```koda
LoadAudioStream(sampleRate, sampleSize, channels);
```

```koda
LoadAutomationEventList(fileName);
```

```koda
LoadDirectoryFiles(dirPath);
```

```koda
LoadDirectoryFilesEx(basePath, filter, scanSubdirs);
```

```koda
LoadDroppedFiles();
```

_…and 56 more in [api_reference.md](api_reference.md)._

## Memory

```koda
MemFree(ptr);
```

## Miscellaneous

```koda
AttachAudioMixedProcessor(processor);
```

```koda
AttachAudioStreamProcessor(stream, processor);
```

```koda
ChangeDirectory(dirPath);
```

```koda
ClearBackground(color);
```

```koda
ClearWindowState(flags);
```

_…and 48 more in [api_reference.md](api_reference.md)._

## Setters

```koda
SetAudioStreamBufferSizeDefault(size);
```

```koda
SetAudioStreamCallback(stream, callback);
```

```koda
SetAudioStreamPan(stream, pan);
```

```koda
SetAudioStreamPitch(stream, pitch);
```

```koda
SetAudioStreamVolume(stream, volume);
```

_…and 49 more in [api_reference.md](api_reference.md)._

## Text

```koda
TextAppend(text, append, position);
```

```koda
TextCopy(dst, src);
```

```koda
TextFindIndex(text, search);
```

```koda
TextIsEqual(text1, text2);
```

```koda
TextLength(text);
```

_…and 2 more in [api_reference.md](api_reference.md)._

## Textures

```koda
ImageAlphaClear(image, color, threshold);
```

```koda
ImageAlphaCrop(image, threshold);
```

```koda
ImageAlphaMask(image, alphaMask);
```

```koda
ImageAlphaPremultiply(image);
```

```koda
ImageBlurGaussian(image, blurSize);
```

_…and 47 more in [api_reference.md](api_reference.md)._

## UI

```koda
HideCursor();
```

```koda
ShowCursor();
```

## Update

```koda
UpdateAudioStream(stream, data, frameCount);
```

```koda
UpdateCamera(camera, mode);
```

```koda
UpdateCameraPro(camera, movement, rotation, zoom);
```

```koda
UpdateMeshBuffer(mesh, index, data, dataSize, offset);
```

```koda
UpdateModelAnimation(model, anim, frame);
```

_…and 5 more in [api_reference.md](api_reference.md)._

## Window

```koda
InitWindow(width, height, title);
```

```koda
WindowShouldClose();
```

