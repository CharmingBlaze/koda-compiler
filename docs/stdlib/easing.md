# @easing — animation curves

Pure Koda easing functions in `stdlib/easing.koda`.

```koda
let easing = import "@easing";
let t = 0.5;
let y = easing.easeInOut(t);
```

Includes `easeIn`, `easeOut`, `easeInOut`, `elastic`, `bounce`, and others. See `stdlib/easing.koda`.
